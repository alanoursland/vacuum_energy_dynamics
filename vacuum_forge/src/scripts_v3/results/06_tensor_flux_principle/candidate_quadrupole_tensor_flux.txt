
========================================================================================================================
Candidate Quadrupole Tensor Flux
========================================================================================================================

========================================================================================================================
Case 0: Quadrupole tensor-flux problem statement
========================================================================================================================
Scalar channel:
  monopole mass -> A-flux

Tensor channel target:
  quadrupole trace-free source -> h_ij^TT radiation

Need to distinguish:
  wave amplitude source ~ second time derivative of quadrupole
  radiated power proxy ~ third time derivative squared

[PASS] quadrupole tensor-flux problem posed

========================================================================================================================
Case 1: Define trace-free quadrupole tensor
========================================================================================================================
General symmetric quadrupole tensor Q:
Matrix([[Q_xx, Q_xy, Q_xz], [Q_xy, Q_yy, Q_yz], [Q_xz, Q_yz, Q_zz]])

Tr(Q) = Q_xx + Q_yy + Q_zz

Trace-free quadrupole Q_TF = Q - Tr(Q) I/3:
Matrix([[2*Q_xx/3 - Q_yy/3 - Q_zz/3, Q_xy, Q_xz], [Q_xy, -Q_xx/3 + 2*Q_yy/3 - Q_zz/3, Q_yz], [Q_xz, Q_yz, -Q_xx/3 - Q_yy/3 + 2*Q_zz/3]])

Tr(Q_TF) = 0
[PASS] Q_TF is trace-free

========================================================================================================================
Case 2: Monopole and dipole are not TT radiation channels
========================================================================================================================
Monopole:
  M is scalar.
  It sources A-flux, not TT tensor radiation.

Dipole:
  D = (D_x, D_y, D_z)
  For isolated systems, ordinary mass dipole radiation is removed by
  center-of-mass / momentum conservation.

Tensor radiation starts at trace-free quadrupole order.

[PASS] monopole belongs to scalar channel
[PASS] dipole is not the leading isolated tensor radiation channel

========================================================================================================================
Case 3: Project Q_TF onto plus/cross basis
========================================================================================================================
Q_plus projection = Q_xx/2 - Q_yy/2
Q_cross projection = Q_xy

For z propagation:
  plus source sees (Q_xx - Q_yy)/2
  cross source sees Q_xy
[PASS] quadrupole projects onto plus/cross channels

========================================================================================================================
Case 4: Time-dependent quadrupole source proxy
========================================================================================================================
Q_plus(t) = Q0*cos(2*Omega*t)
Q_cross(t) = Q0*sin(2*Omega*t)

Q_plus_ddot = -4*Omega**2*Q0*cos(2*Omega*t)
Q_cross_ddot = -4*Omega**2*Q0*sin(2*Omega*t)

Q_plus_dddot = 8*Omega**3*Q0*sin(2*Omega*t)
Q_cross_dddot = -8*Omega**3*Q0*cos(2*Omega*t)

amplitude-source proxy Q_ddot² sum = 16*Omega**4*Q0**2
power proxy Q_dddot² sum = 64*Omega**6*Q0**2
[PASS] quadrupole amplitude proxy uses second derivatives
[PASS] quadrupole power proxy uses third derivatives

========================================================================================================================
Case 5: Static quadrupole does not radiate
========================================================================================================================
Q_static = Q0
Q_ddot = 0
Q_dddot = 0
[PASS] static quadrupole has no wave-amplitude source
[PASS] static quadrupole has no radiation-power proxy

========================================================================================================================
Case 6: Tensor-flux analogy
========================================================================================================================
Scalar monopole channel:

  M -> A-flux
  F_A = 8*pi*G*M/c^2

Tensor quadrupole channel:

  Q_ij^TF -> h_ij^TT
  changing quadrupole -> outgoing tensor radiation

Interpretive upgrade:

  A-flux is monopole scalar vacuum flow.
  Tensor flux is quadrupole TT radiative vacuum flow.

[PASS] tensor-flux analogy stated

========================================================================================================================
Case 7: Classification
========================================================================================================================
Results:

1. Trace-free quadrupole Q_ij^TF is the natural tensor source object.
2. Monopole belongs to scalar A-flux channel.
3. Dipole is not the leading isolated tensor radiation channel.
4. Q_ij^TF projects onto plus/cross TT polarizations.
5. Wave amplitude source is associated with second derivatives.
6. Radiated-power proxy is associated with third derivatives squared.
7. Static quadrupole does not radiate.

[PASS] quadrupole tensor-flux source structure passes first checks

========================================================================================================================
Final interpretation
========================================================================================================================
This script gives the first source-side structure for the tensor-flux
program.

A-flux uses monopole mass.
Tensor flux uses trace-free quadrupole structure.

The plus/cross radiative channels are sourced by the TT projection of
the changing quadrupole.

Next steps:
  normalize the quadrupole coupling
  connect to far-zone wave amplitude
  develop tensor radiation energy flux
  avoid unwanted scalar radiation

Possible next artifact:
  candidate_quadrupole_tensor_flux.md

Possible next script:
  candidate_tensor_flux_principle.py
