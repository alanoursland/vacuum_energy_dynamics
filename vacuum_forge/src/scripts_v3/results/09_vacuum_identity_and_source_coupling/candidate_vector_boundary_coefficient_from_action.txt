
========================================================================================================================
Candidate Vector Boundary Coefficient From Action
========================================================================================================================
[INFO] Archive dependency check:
  - vector_boundary_value_problem_marker: dependency_satisfied (Upstream derivation exists; output not verified (no expected_output declared).)

========================================================================================================================
Case 0: Vector boundary coefficient from action problem
========================================================================================================================
Question:

  Can the exterior/boundary coefficient be tied to the curl-energy
  source ratio alpha_W/(2K_c)?

Rules:

  keep all coefficients symbolic
  do not insert Lense-Thirring normalization
  check whether C_b is a new free coefficient or controlled by alpha_W/K_c
[WARN] boundary coefficient problem posed: CONSTRAINED_BY_IDENTITY

========================================================================================================================
Case 1: Curl-energy source ratio
========================================================================================================================
Curl-energy equation:

  curl curl W = - alpha_W j_T/(2K_c)

Coefficient ratio:

lambda_eq = alpha_W/(2*K_c)

This ratio is still missing, but it should control the exterior amplitude.
[FAIL] field-equation coefficient ratio identified: MISSING — alpha_W/(2K_c) not derived

========================================================================================================================
Case 2: Green-function amplitude
========================================================================================================================
Under transverse gauge:

  Delta W = alpha_W j_T/(2K_c)

Poisson Green-function amplitude conventionally introduces 1/(4*pi):

  W ~ [alpha_W/(8*pi K_c)] integral j_T/|x-x'| d^3x'

lambda_green = alpha_W/(8*pi*K_c)

This is not a GR coefficient.
It only relates exterior amplitude to the missing action ratio.
[WARN] Green-function amplitude tied to alpha_W/K_c: CONSTRAINED_BY_IDENTITY — absolute ratio still missing

========================================================================================================================
Case 3: Far-field relation to angular momentum
========================================================================================================================
For a compact rotating source, the far-field vector amplitude should be:

  C_J J

where:

  C_J = C_shape * alpha_W/(8*pi K_c)

C_J = C_shape*alpha_W/(8*pi*K_c)

C_shape depends on angular conventions and source model.
alpha_W/K_c remains the physical missing ratio.
[WARN] far-field coefficient tied to action ratio times shape factor: CONSTRAINED_BY_IDENTITY — shape factor and action ratio not fully derived

========================================================================================================================
Case 4: Boundary coefficient relation
========================================================================================================================
Boundary coefficient can be parameterized as:

  C_b = C_shape * alpha_W/(8*pi K_c)

C_b = C_shape*alpha_W/(8*pi*K_c)

This means C_b should not be an independent new knob if the action
and source model are specified.

But C_shape and alpha_W/K_c are still missing.
[WARN] boundary coefficient reduced to action ratio plus shape factor: CONSTRAINED_BY_IDENTITY — not a final normalization

========================================================================================================================
Case 5: Precession coefficient separation
========================================================================================================================
Observable precession coefficient separates into:

  C_total = beta_W * C_shape * alpha_W/(8*pi K_c)

C_total = C_shape*alpha_W*beta_W/(8*pi*K_c)

Meaning:
  vector field normalization and precession coupling are separate.

Missing:
  beta_W
  C_shape
  alpha_W/K_c
[FAIL] precession coefficient separated into missing factors: MISSING — observable normalization not derived

========================================================================================================================
Case 6: No new free boundary knob
========================================================================================================================
Important result:

  C_b should not be treated as an independent free coefficient once
  the vector action, source coupling, and boundary/source model are fixed.

Instead:

  C_b is controlled by alpha_W/K_c and geometry/source shape factors.

Remaining danger:

  if alpha_W/K_c, beta_W, and C_shape are all independently fitted,
  the vector sector becomes a fit machine.
[WARN] boundary knob reduced but not eliminated: CONSTRAINED_BY_IDENTITY — main action ratio remains missing

========================================================================================================================
Case 7: Classification
========================================================================================================================
| Item | Status |
|---|---|
| curl-energy equation ratio alpha_W/(2K_c) | MISSING |
| Green-function amplitude alpha_W/(8pi K_c) | CONSTRAINED_BY_IDENTITY |
| boundary coefficient C_b from action ratio | CONSTRAINED_BY_IDENTITY |
| source/shape factor C_shape | MISSING |
| precession coupling beta_W | MISSING |
| Lense-Thirring normalization | HAND_ASSIGNED if inserted now |

[WARN] boundary coefficient classification produced: CONSTRAINED_BY_IDENTITY — C_b tied to missing action ratio, not solved

========================================================================================================================
Case 8: Next tests
========================================================================================================================
Possible next scripts:

1. candidate_vector_source_shape_factor.py
   Compute source/shape factor for a simple rotating sphere symbolically.

2. candidate_kappa_source_law_from_trace_exchange.py
   Work missing kappa source law.

3. candidate_tensor_source_from_exchange_identity.py
   Work hand-assigned tensor coupling.

Recommended next script:

  candidate_vector_source_shape_factor.py
[WARN] next test selected: CONSTRAINED_BY_IDENTITY — source shape factor is next local vector cleanup

========================================================================================================================
Final interpretation
========================================================================================================================
The boundary coefficient is not a totally new free knob.

It can be related structurally to:

  alpha_W/K_c
  source geometry / shape factor

But the actual normalization is still missing because:

  alpha_W/K_c is missing
  C_shape is missing
  beta_W is missing

Possible next artifact:
  candidate_vector_boundary_coefficient_from_action.md

Possible next script:
  candidate_vector_source_shape_factor.py
