# Lambda and the Global Datum

**Claim (forge-verified; unimodular arc 033–037).** Λ's status,
meaning, and decoupling from the vacuum's energy are settled. Its value
is a single global datum the local theory cannot supply — and under
P10 that is a *conclusion*, not a disappointment: the theory explains
why every local selector had to fail.

## 1. Status: an Integration Constant

P3, read through the exact identity
$\kappa = \ln(\sqrt{-g}/\sqrt{-\bar g})$, is the unimodular constraint:
the vacuum's volume mode is fixed, not dynamical (033) — and the
packing explains *why* it must be externally fixed (the microphysics
gives it no restoring force; `02_gravitational_response.md` §3).

Under the constrained variation, the response tensor is the trace-free
Einstein combination — the one Lovelock hypothesis VED relaxes (H3,
the unimodular relaxation, the mildest available). The Bianchi identity
plus matter conservation then yield

$$R + \tfrac{8\pi G}{c^4}T = 4\Lambda = \text{const}:$$

**Λ enters as a constant of integration of the conservation law, not
as a coupling in the action** (033; covariantly: the constraint's
multiplier is forced constant on shell, 034). The full equations
$G_{ab} + \Lambda g_{ab} = (8\pi G/c^4)T_{ab}$ are reconstructed
exactly, so nothing in `04_field_equations/` moves.

This retroactively explains the Λ-selector sweep (development
derivations 008–016): six independent local routes — variational,
boundary, topology, measure, relaxation, frustration-microphysics —
each failed to value Λ without importing a scale. That is the defining
structural property of an integration constant. *The sweep was an
empirical rediscovery of Λ's unimodular status.*

## 2. Meaning: the Ground Configuration's Curvature

Since $4\Lambda = R + (8\pi G/c^4)T$ is a constant of motion, the datum
is one slice's worth of that combination — and in a vacuum epoch the
floor's density cancels out of it exactly (sequestering), leaving

$$\Lambda = \tfrac14 R_{\text{ground}}:$$

**Λ measures the intrinsic curvature of the vacuum's ground
configuration — geometry, not energy** (036).

## 3. Sign and Magnitude: What the Packing Says

The exact relief function (037), $\cos\delta(s) = \cos s/(1+2\cos s)$:

- **Sign (conditional theorem).** Spherical curvature monotonically
  relieves tetrahedral frustration (the deficit closes exactly on the
  600-cell); hyperbolic curvature strictly aggravates it. *If* the
  ground state curved at all to relieve frustration, it would curve
  spherically: Λ ≥ 0.
- **Magnitude (kill, and the coherent branch).** Relief is
  quadratic-flat: $\Delta(s) = \Delta_0 - \tfrac{5\sqrt2}{24}s^2 +
  O(s^4)$. Curvature small enough to match the observed Λ relieves
  ~$10^{-122}$ of the frustration — essentially none — while
  meaningful relief demands packing-scale curvature (Λ ~ $10^{122}$
  too large). No intermediate regime exists. The partial-relief route
  to Λ's value is dead, and the coherent branch is the one the theory
  occupies: **a flat ground state (Λ_ground = 0) with its frustration
  retained and sequestered.**

## 4. Value: External, and Honestly So

The observed Λ is therefore the global datum — in the
Henneaux–Teitelboim form, the variable conjugate to the total
four-volume; a boundary/initial condition of the universe, decoupled
from the floor (035, 037). The vacuum sector's considered position:

```text
Lambda's value is not the vacuum's to derive — any more than the
universe's total mass is. The theory owes an account of every LOCAL
quantity; Lambda is not one. Three independent structures said so
(the unimodular constraint, the sequestering theorem, the relief
geometry), and the program records their agreement as the answer.
```

What survives as live physics adjacent to Λ: the F1 leak — the
matter-era violation of the unimodular constraint,
$AB - 1 = \tfrac32\Omega_m(H_0r/c)^2$, re-derived in three lines from
the covariant sourced equation
$\kappa' = -(rB/2N)(T^t{}_t - T^r{}_r)$ with the comoving dust stress
(034). It is ~$6\times10^{-31}$ at 1 AU: an honesty entry, not a
target.

## Verification Pointers

```text
033  unimodular break, Lambda as integration constant, sequestering
034  covariant constraint, multiplier = Lambda, F1 re-derivation
035  floor decoupling (branch decision)
036  datum = ground curvature; sign analysis
037  exact relief function; partial-relief kill; coherent flat branch
```
