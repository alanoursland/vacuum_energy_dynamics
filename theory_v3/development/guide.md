write the artifact from the script output,
write the next script from the bottleneck,
do not overclaim,
keep theorem targets distinct from laws,
preserve rejected regressions,
end groups with a summary ledger,
then update the field-equation snapshot separately.