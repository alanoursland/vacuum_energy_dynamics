# Boundary counterterm coefficient is fixed by cancellation

the coefficient that cancels leakage is unique.

Leakage plus counterterm:

```text
B_total = b - c b
```

Cancellation fixes `c = 1`.

The coefficient is not free once the leakage normalization is fixed.
