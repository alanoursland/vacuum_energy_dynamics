# Actions differing by total divergence share bulk equations but not boundary data

total divergence changes endpoints, not bulk Euler equations.

A total divergence changes endpoint data but has bulk Euler-Lagrange residual `0`.

Boundary completions can change variational well-posedness without changing bulk equations.
