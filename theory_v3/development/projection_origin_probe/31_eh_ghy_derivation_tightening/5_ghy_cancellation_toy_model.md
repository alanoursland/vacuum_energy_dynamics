# GHY-like boundary completion cancels derivative leakage

a boundary counterterm coefficient is fixed by cancellation.

Leakage plus counterterm:

```text
B_total = b - c b
```

Cancellation fixes `c = 1`.

The coefficient is not free once the leakage normalization is fixed.
