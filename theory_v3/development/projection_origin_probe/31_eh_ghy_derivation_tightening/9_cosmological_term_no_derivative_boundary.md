# Cosmological term has no derivative boundary leakage

Lambda is a volume baseline term with algebraic metric variation.

One-component variation:

```text
delta(Lambda sqrt(g)) = Lambda*h/(2*sqrt(g))
```

No derivative of the metric variation appears, so Lambda introduces no GHY-like boundary leakage.
