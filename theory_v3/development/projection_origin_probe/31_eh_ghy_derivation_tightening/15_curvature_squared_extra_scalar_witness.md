# Curvature-squared branch carries an extra scalar witness

a toy characteristic factor exposes an additional mode.

Toy characteristic factor:

```text
k**2*(k**2 + m**2)
```

The extra factor beyond `k^2=0` witnesses an additional mode.
