# Second-derivative actions create boundary derivative leakage

Dirichlet field data alone does not kill derivative variation leakage.

Boundary model: `3 delta q + 5 delta q'`.

With Dirichlet `delta q=0`, residual is `5*B1`.

So second-derivative actions require boundary completion if only field values are fixed.
