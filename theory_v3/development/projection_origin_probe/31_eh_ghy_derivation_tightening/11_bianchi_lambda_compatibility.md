# Lambda term is divergence-compatible under metric compatibility

constant Lambda is compatible with Bianchi/stress conservation.

Schematic divergence: `nabla(Lambda g) = Lambda nabla g + (nabla Lambda) g`.

For metric compatibility and constant Lambda, residual is `0`.
