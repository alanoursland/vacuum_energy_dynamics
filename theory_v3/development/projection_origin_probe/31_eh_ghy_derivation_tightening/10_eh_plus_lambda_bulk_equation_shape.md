# EH plus Lambda produces Einstein tensor plus baseline term

G + Lambda g = T is a baseline-shifted curvature equation.

Equation shape:

```text
G + Lambda g = T
G = -Lambda*g + T
```

Lambda is a baseline shift in the metric equation.
