# GHY normalization tracks the EH bulk normalization

the required boundary normalization scales with the EH leakage coefficient.

If EH leakage has coefficient `K`, cancellation fixes boundary coefficient:

```text
c_GHY = K
```
