# Dirichlet metric data becomes well posed after boundary completion

after completion the remaining boundary variation is proportional to fixed boundary data.

Completed boundary term: `2 delta q`.

Under fixed boundary data, residual is `0`.
