# General R Moment Ladder

Adding endpoint contact weight `(1-y)^R` shifts the adjacent moment ratio.

```text
C_R[P] = ∫ P(y)(1-y)^(R+1)y^(-1/2)dy
r_(R,k) = (2k - 1)/(2k + 2R + 3).
```

So `R` classifies the chosen contact/test-pairing ladder.
