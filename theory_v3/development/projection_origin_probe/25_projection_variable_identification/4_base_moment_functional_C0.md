# Base Moment Functional C0

The observed projection ladder uses the base functional

```text
C_0[P] = ∫_0^1 P(y) (1-y) y^(-1/2) dy.
```

For monomials,

```text
C_0[y^k] = B(k+1/2, 2).
```

The ratio of adjacent moments gives the observed coefficient.
