# 12. Symmetric Tensor Rank Gap

A symmetric bilinear form on an `m`-dimensional space has

```text
m(m+1)/2
```

components. A scalar trace channel has one component. The per-mode rank gap is
therefore

```text
m(m+1)/2 - 1.
```

For `m=3`, the gap is `5`.
