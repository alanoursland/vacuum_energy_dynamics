# Vacuum Dimension Selector: Provisional Proof Chain

## Folder Role

This folder is the third selector-level proof chain after:

```text
vacuum_interval_directional_probe_origin
torsion_defect_exclusion
```

The previous selector folders established conditional gates:

```text
directional interval Hessian data can supply metric-like tensor data;
torsion-free EH is justified when total torsion source is absent.
```

This folder tests the next selector:

```text
Why 3 spatial dimensions plus 1 time dimension?
```

Earlier folders already showed that several accepted physical targets converge
on 3+1. This folder must be stricter. It should separate:

```text
consistency with 3+1
```

from:

```text
derivation of 3+1 from the vacuum ontology or action gates.
```

## Handoff From Earlier Folders

Relevant existing reports:

```text
boundary_flux_field_bridge/49_dimension_dependence_inverse_square.md
vacuum_action_origin/30_dimensionality_selector_gate.md
vacuum_action_origin/55_dimensionality_selector_consistency.md
vacuum_action_origin/57_remaining_physical_selectors_status.md
einstein_hilbert_origin_tests/125_ontology_to_eh_assumption_table.md
torsion_defect_exclusion/29_torsion_defect_exclusion_conclusion.md
```

The already-established consistency checks are:

```text
inverse-square boundary flux -> n = 3 spatial dimensions;
one propagation time dimension -> D = 4 spacetime dimensions;
two massless spin-2 polarizations -> D = 4;
4D Lovelock gate -> EH is the unique dynamical local metric curvature term
                    beyond Lambda/topology.
```

The open gap is:

```text
why these selector criteria are physically primary rather than merely matched.
```

## Desired Endpoint

Strong closeout:

```text
the vacuum ontology supplies:
  one universal clock/ordering channel;
  three independent long-range flux directions;
  a massless spin-2 metric branch with two polarizations;
  local second-order metric action assumptions.

Together these force:
  n = 3, D = 4.
```

Honest weaker closeout:

```text
3+1 is multiply selected by the accepted physics gates, but the vacuum
ontology has not independently derived why those gates are fundamental.
```

Rejected move:

```text
assume four dimensions because EH/GR is the target.
```

That would be circular.

## Proof Batch 1: Flux Dimension Gate

Purpose: restate the radial flux result as the first local theorem in this
folder.

```text
1. n_dimensional_flux_scaling
   Prove conserved radial flux gives field strength F(r) ~ r^(1-n).

2. inverse_square_selects_three_space
   Solve 1-n = -2 -> n = 3.

3. green_function_dimension_classes
   Separate n=1, n=2 logarithmic, and n>2 power-law Green behavior.

4. flux_dimension_not_ontology_derivation
   Record that inverse-square selects n=3 only if inverse-square long-range
   flux is a fundamental target.

5. flux_dimension_gate_status
   Status: flux gate selects 3 spatial dimensions conditionally.
```

## Proof Batch 2: Time Channel And Lorentzian Extension

Purpose: separate spatial dimension from spacetime dimension.

```text
6. one_clock_channel_adds_one_time_dimension
   If there is one universal propagation/clock channel, D = n + 1.

7. multiple_time_channel_obstruction
   Show more than one independent time channel changes signature and mode
   counting rather than silently preserving the same field theory.

8. zero_time_channel_obstruction
   Show a purely spatial elliptic model has no propagation branch.

9. lorentzian_signature_selector_gate
   State the conditional selector: one time channel plus three flux dimensions
   gives 3+1.

10. time_channel_status
   Status: D=4 follows from n=3 plus one clock channel, but the one-clock
   ontology remains a selector assumption unless derived.
```

## Proof Batch 3: Metric Tensor And Polarization Count

Purpose: test the spin-2/polarization selector without overclaiming it.

```text
11. symmetric_metric_component_count
   Count D(D+1)/2 metric components.

12. diffeomorphism_gauge_reduction_count
   Reproduce the massless spin-2 physical degree formula D(D-3)/2.

13. two_polarization_selector
   Solve D(D-3)/2 = 2 and keep the positive physical solution D = 4.

14. scalar_and_vector_mode_mismatch
   Show scalar and vector branches do not reproduce two spin-2 polarizations.

15. polarization_selector_status
   Status: two-polarization massless spin-2 branch selects D=4 conditionally.
```

## Proof Batch 4: Local Metric Action / Lovelock Gate

Purpose: test why dimension matters for the nonlinear action completion.

```text
16. lovelock_order_dimension_table
   Classify Lovelock order p by dimension D: dynamical, topological, or
   vanishing.

17. four_dimensional_eh_uniqueness_gate
   Prove in D=4: Lambda is algebraic, EH is dynamical, Gauss-Bonnet is
   topological, higher Lovelock terms vanish.

18. higher_dimension_lovelock_branch
   Show D>4 permits additional Lovelock dynamics, so EH uniqueness is no
   longer automatic under the same assumptions.

19. lower_dimension_spin2_obstruction
   Show D<4 lacks the same local two-polarization spin-2 branch.

20. action_dimension_status
   Status: local second-order metric action assumptions make D=4 special.
```

## Proof Batch 5: Boundary / Induced Metric Dimension Coupling

Purpose: connect the dimension selector to the boundary/tensor results.

```text
21. boundary_dimension_relation
   Separate the counts: spatial slices have dimension D-1, spacetime boundary
   hypersurfaces have dimension D-1, and spatial boundary surfaces have
   dimension D-2.

22. induced_metric_component_count_by_boundary_dimension
   Count m(m+1)/2 induced boundary metric components.

23. three_boundary_component_match
   For m=3, induced metric has six components, matching the directional probe
   reconstruction used by the tensor-boundary selector.

24. scalar_projection_rank_gap_by_dimension
   Show scalar projection data is increasingly insufficient as boundary
   dimension grows.

25. boundary_dimension_status
   Status: 3D boundary metric data matches the already-built directional
   selector, but this is consistency rather than full ontology derivation.
```

## Proof Batch 6: Selector Synthesis And Closeout

Purpose: close the folder honestly.

```text
26. independent_selector_intersection
   Intersect the accepted conditional selectors:
     inverse-square flux,
     one time channel,
     two spin-2 polarizations,
     4D Lovelock uniqueness.

27. dimension_selector_dependency_table
   Table each selector, assumption, result, and remaining open ontology input.

28. hidden_dimension_assumption_witness
   Show where assuming D=4 would make the proof circular.

29. vacuum_dimension_selector_conclusion
   Close folder:
     D=4 is strongly overdetermined by the conditional gates;
     independent derivation requires a vacuum ontology reason for the gates.
```

## What Would Count As A Strong Result

Strong result:

```text
The vacuum ontology independently supplies:
  one clock channel;
  three conserved long-range flux directions;
  symmetric metric Hessian data;
  two physical transverse metric modes;
  locality/second-order action assumptions.

Then D=4 is derived as the intersection of those selectors.
```

Useful but weaker result:

```text
The proof chain shows that all accepted physical gates converge on D=4, while
clearly marking which ontology inputs remain unproved.
```

Rejected move:

```text
start from 4D GR and read the dimension back into the vacuum model.
```

This folder exists to prevent that circularity.
