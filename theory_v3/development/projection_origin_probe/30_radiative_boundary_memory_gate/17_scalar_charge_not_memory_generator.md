# Scalar charge is not memory generator

This independence witness records that a scalar charge variable `M` is not by itself a memory generator. The tensor memory norm varies in `(C_+, C_x)`, not in the scalar monopole variable.
