# 11. News from TT derivative gate

Boundary news is modeled as a retarded-time derivative of shear/TT strain:

```text
h(u) = A cos(wu),
N(u) = ∂_u h = -A w sin(wu).
```

Validated check:

```text
N^2 != 0
```

Result: radiative boundary flux is generated by tensor-time variation, not scalar monopole charge.
