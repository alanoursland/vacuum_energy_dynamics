# 12. Memory as integrated news transport gate

A simple exact witness uses constant news over an interval:

```text
N(u)=A,  0 <= u <= U.
```

The memory is

```text
Δh = ∫_0^U N du = A U.
```

Result: memory is an accumulated tensor boundary displacement, not a scalar charge.
