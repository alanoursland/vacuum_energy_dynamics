# 2. Current Closure Ledger

This document records what is already closed enough that it should not be re-litigated as if it were still the frontier.

## Closed or strongly closed

The original scalar coefficient is solved:

```text
r_k = (2k - 1)/(2k + 3)
```

is the base `R=0` case of the endpoint-contact / moment-kernel ladder:

```text
r_(R,k) = (2k - 1)/(2k + 2R + 3).
```

The scalar projection ladder is a trace/monopole/Newtonian boundary-admissibility sector.

The scalar boundary ledger cannot reconstruct traceless shear, Weyl curvature, TT radiation, or full tensor boundary data.

Scalar nonlinear dressing cannot reproduce the full GR post-Newtonian tensor sector.

The boundary is a reduction and ledger interface. It is not the underlying ontology.

Finite scalar flux fixes the physical `1/r` exterior class but does not by itself specify the projection contact index `R`; that index belongs to a chosen compactified moment/test embedding.

## Conditionally closed

Exact quadratic/parallelogram response gives metric reconstruction.

Shared metric interval dependence of matter action gives the stress-tensor source route.

Absence of torsion/nonmetricity sources selects the minimal Levi-Civita branch.

Metric plus diffeomorphism plus second-order locality plus boundary differentiability plus no extra fields gives EH/GHY as the minimal GR action branch in `D=4`.

Asymptotically flat finite scalar flux selects the `Lambda = 0` branch, but nonzero `Lambda` remains an allowed global/vacuum-baseline branch.

## Localized frontier

The remaining field-equation problem is no longer spread across the archive. It has been compressed into the strain/gradient sector:

```text
K_strain = K_EH + ε K_residual.
```

The prior work constrains this residual hard. It is not an arbitrary menu item. Any residual must preserve or explicitly route locality, calibration coherence, diffeomorphism invariance, Lorentzian hyperbolicity, two TT degrees of freedom, boundary differentiability, the symplectic radiation ledger, and source purity.

## Still open

Whether the accumulated gates force `ε = 0`.

Whether they permit a controlled `ε != 0`.

Whether an additional strain axiom is required.

Matter microstructure remains outside the stress-coupling result.

Quantum structure remains outside the classical chain.

Lambda relaxation remains outside local scalar flux accounting.
