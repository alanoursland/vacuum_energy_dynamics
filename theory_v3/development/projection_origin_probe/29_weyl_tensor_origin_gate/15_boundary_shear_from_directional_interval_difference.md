# 15. Boundary shear from directional interval difference

For boundary interval matrix

```text
H = diag(1+s, 1-s),
```

trace data is constant, but directional difference gives

```text
(Q(e1)-Q(e2))/2 = s.
```

Boundary shear is directional interval data.
