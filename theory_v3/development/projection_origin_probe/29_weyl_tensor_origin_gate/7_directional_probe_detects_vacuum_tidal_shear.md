# 7. Directional probe detects vacuum tidal shear

For trace-free tidal matrix

```text
H = diag(a,-a,0),
```

scalar trace gives zero, but directional probes give

```text
Q(e1)-Q(e2) = 2*a.
```

Thus directional interval probes detect the data the scalar ledger misses.
