# Strain Axiom Candidate Sieve

## Purpose

This sieve applies the minimal strain axiom contract to the currently named
routes. It does not adopt an axiom.

It is paired with:

```text
strain_axiom_candidate_sieve_vacuumforge.md
```

## Result

No currently named nonbaseline strain axiom satisfies the minimal contract.

The only passing route is the baseline/null route:

```text
no new axiom, retain EH/GHY baseline at epsilon = 0
```

That route is not a new strain axiom and licenses no nonbaseline vacuum-sector
physics.

## Candidate Status

```text
primitive nonmetric X:
  open but incomplete; naming X is not a strain axiom

primitive mismatch:
  open but incomplete; naming mismatch is not a variational branch

nonlocal relaxation:
  deferred until local GR limit, conservation, and source quarantine are supplied

boundary/global selector:
  deferred; class restriction is not K_strain selection

metric relabeling:
  rejected as ontology derivation

mechanism-fit axiom:
  rejected as post-hoc rescue
```

## Consequence

The program is now at an adoption decision:

```text
adopt a fully specified new strain axiom;
or keep nonbaseline vacuum-sector mechanisms quarantined.
```

## Next Obligation

```text
strain_axiom_adoption_decision_required_032
```
