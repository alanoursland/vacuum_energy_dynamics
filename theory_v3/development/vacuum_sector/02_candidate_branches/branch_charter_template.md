# Branch Charter Template

Copy this template for each candidate strain branch.

```text
branch name:

purpose:

X variable:

metric reduction map:

neighboring mismatch rule:

candidate invariant/scalar:

boundary term or boundary data:

matter coupling route:

expected epsilon status:

accumulated gates:
  metric reduction:
  nonquadratic routing:
  calibration coherence:
  diffeomorphism/relabeling:
  Lorentzian hyperbolicity:
  two TT modes:
  boundary differentiability:
  symplectic radiation ledger:
  source-ledger purity:

known risks:

kill condition:

first concrete test:

status:
```

Allowed status labels:

```text
admissible at epsilon = 0
controlled epsilon != 0 possible
fails accumulated gate
underdetermined without new axiom
not yet evaluated
```

## Charter Rule

No branch should be used to explain `Lambda`, dark-sector behavior,
Casimir/UFFT channels, or interiors until it has a seat in the strain dynamics
or an explicit quarantine outside the closed gravitational equations.
