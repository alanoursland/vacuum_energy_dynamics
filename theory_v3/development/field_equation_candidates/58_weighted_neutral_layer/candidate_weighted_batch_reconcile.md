# candidate_weighted_batch_reconcile — Result Note

## Result

The reconciliation confirms that Group 58 successfully constructed a nontrivial weighted-neutral finite-layer route without upgrading the trace-normalization candidate.

The group is ready for result notes and a status summary.

## Main Findings

The stable Group 58 result is:

```text
Group 58 directly attacked weighted layer neutrality;
rho=rho0*(1-y^2)^2*(y-c*) with c*=2Rell/(7R^2+ell^2) is weighted-neutral;
flat odd cancellation is rejected as sufficient;
weighted-neutral profile has finite explicit reduced energy;
weighted Q=0 kills reduced tail and mass-shift diagnostics;
weighted shape supports reduced D=0 closure;
weighted-neutral route survives conditionally;
physical use remains blocked.
```

The reconciliation also preserves the necessary non-results:

```text
not insertion;
not full neutrality theorem;
not covariant theorem;
not parent closure.
```

## Boundary

No `B_s/F_zeta` insertion occurred. No active `O` was constructed. No recombination or parent closure opened.

## Steering Consequence

The next script should be `candidate_group_58_status_summary.py`. The summary should emphasize that Group 58 answered the weighted-neutrality blocker constructively, while keeping source safety, covariant lift, candidate-term audit, and physical use blocked.
