# candidate_next_route_priority — Result Note

## Result

The script prioritizes the next route after the parent-blocker inventory.

Priority order:

```text
1. source_trace_divergence_blocker_audit
2. boundary_diagnostic_ledger
3. active_O_necessity_or_rejection
4. trace_normalization_decision
99. parent_equation_construction
```

## Main Findings

The top priority is:

```text
source_trace_divergence_blocker_audit
```

This is the right next target because it attacks the central parent blockers before recombination:

```text
source count;
trace count;
divergence identity.
```

The boundary diagnostic ledger is useful but secondary. It preserves diagnostic constraints cleanly, but it does not by itself address parent count/conservation.

Active `O` is important but should not be built just because it has a name. Its necessity should be driven by source/trace/divergence pressure.

Trace-normalization decision remains important, but it still depends on safety burdens.

Parent-equation construction is ranked premature:

```text
rank = 99
```

Rejected shortcuts:

```text
parent next;
transition loop;
O-first by label.
```

## Boundary

This prioritizes next work. It does not solve any blocker.

## Steering Consequence

Proceed to the parent-inventory classifier. The group has enough evidence to close with parent equation blocked and Group 67 pointed at source/trace/divergence blockers.
