# Candidate Safe-Membership Relation Family Sieve

## Result

The script classifies `zeta_Bs -> T_zeta` relation families.

It preserves `zeta_Bs -> T_zeta` as diagnostic / compatible-if-declared only and installs no active membership.

## Main Findings

- `zeta_Bs -> T_zeta` may remain a diagnostic compatibility label with no source, metric, divergence, projector, theorem, adoption, insertion, or parent effect.
- `zeta_Bs -> T_zeta` may remain compatible-if-declared, but not declaration-ready or theorem-ready.
- Branch-sensitive cases may be carried in parallel as `zeta_Bs_metric -> T_zeta_metric` and `zeta_Bs_scale -> T_zeta_scale`.
- A later membership declaration route may exist only if object, sector, domain/codomain, criteria, role-purity, branch-status, and exclusions close.
- A membership theorem would require actual derivation beyond relation naming.
- Membership used as trace/residual zero-incidence is eliminated.
- Membership used as active `O`, projector, or `B_s/F_zeta` insertion support is eliminated.

## Boundary

Compatible-if-declared is not declared membership. Relation syntax is not proof. Membership is not incidence, no-overlap, residual control, active `O`, projection, or insertion support.

## Safe Handoff

Run `candidate_spatial_response_equation_family_sieve.py` next.
