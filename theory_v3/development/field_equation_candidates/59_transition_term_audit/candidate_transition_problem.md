# candidate_transition_problem — Result Note

## Result

The opener correctly frames Group 59 as a transition-term audit, not an insertion group.

The group may generate and classify candidate transition-response surfaces from the finite-layer residues and weighted-neutral layer shape, but it must not insert `B_s/F_zeta`, insert residues as repair tensors, construct active `O`, or open a parent equation.

## Main Findings

The opener preserves the correct inheritance from Group 58:

```text
weighted-neutral layer route survives conditionally;
weighted-neutral layer is available as a test shape;
physical use remains blocked.
```

It also states the core discipline for this group:

```text
candidate transition terms must pass locality;
weighted neutrality;
source/trace incidence;
divergence filters.
```

That matters because Group 59 is the first group that starts treating boundary-layer structures as candidate equation ingredients. The opener prevents the dangerous shortcut:

```text
candidate clue -> inserted term
```

## Rejected Shortcuts

The opener rejects:

```text
residue insertion;
weighted profile insertion;
candidate-by-name without filters.
```

These rejections are essential. Without them, transition residues could become unlicensed repair tensors.

## Boundary

No term is adopted. No insertion occurs. No active `O`, recombination, or parent closure is opened.

## Steering Consequence

Proceed to candidate inventory. The next step is to name the available transition ingredients — residues, weighted-neutral shape, and stress basis — and then filter them.
