# candidate_hierarchy_formula_route_classifier — Analysis Note

## Result

`candidate_hierarchy_formula_route_classifier.py` records the stable Group 88 statuses:

```text
MOMENT_RATIO_IDENTITY_DERIVED
BETA_LINEAR_SYSTEM_DERIVED
CRAMER_COEFFICIENT_FORMULA_DERIVED
FORMULA_VALIDATED_N1_TO_N6
NEXT_OBSTRUCTION_PERSISTS_N1_TO_N6
FINITE_HIERARCHY_FORMULA_STRENGTHENED
ALL_ORDER_DETERMINANT_NONZERO_OPEN
ALL_ORDER_LIMIT_OPEN
LOCAL_RHO_NONZERO_REMAINS
PHYSICAL_COVARIANT_ORIGIN_OPEN
PARENT_DIVERGENCE_UNPROVEN
RECOMBINATION_BLOCKED
```

## Interpretation

This classifier is correct.

Group 88 upgrades the finite hierarchy from:

```text
pattern observed through N=4
```

to:

```text
finite-N Beta/Cramer coefficient formula derived and validated through N=6.
```

That is a major technical strengthening.

But the classifier also preserves the crucial boundary:

```text
formula is not all-order closure.
```

There are three remaining mathematical burdens:

```text
det(A_N) nonzero for all N;
coefficient recurrence or orthogonal-polynomial form;
all-order limit/convergence behavior.
```

And three physical/theory burdens:

```text
local rho nonzero;
physical/covariant origin;
parent divergence/recombination.
```

## What Changed

The exactness hierarchy now has a formula-level generator.

This is no longer just a list of profiles. It is a finite-N construction.

## What Did Not Change

The hierarchy is still not a theorem of physical local inertness. It remains a reduced finite-order suppression mechanism.

## Steering Consequence

The next group should not simply validate `N=7` and `N=8`. The better next target is one of:

```text
89_all_order_determinant_test
89_hierarchy_recurrence_search
89_all_order_limit_obstruction
```

My preference is the determinant test, because existence/uniqueness for all `N` is the cleanest next mathematical gate.
