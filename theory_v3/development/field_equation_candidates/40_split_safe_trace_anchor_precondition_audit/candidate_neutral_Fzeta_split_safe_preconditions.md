# Candidate Neutral F_zeta Split-Safe Preconditions

## Result

The script states the split-safe conditions under which neutral `F_zeta` may remain available while the active `B_s` branch is deferred.

## Main Findings

- `F_zeta` may remain a neutral response placeholder only while it carries no concrete `zeta/d` or `2*zeta/d` expression.
- Optional `F_zeta_metric` and `F_zeta_scale` variants are only conditional parallel placeholders, not active choices.
- Any concrete expression must be branch-indexed or delayed.
- Neutral `F_zeta` provides no theorem proof, no adoption support, and no insertion support.

## Boundary

Neutral means expression-free. It cannot hide the factor-of-two burden, cannot prove trace normalization, cannot be adopted as Package B, and cannot be inserted into a field equation.

## Safe Handoff

Run `candidate_safe_membership_split_safe_preconditions.py` next.
