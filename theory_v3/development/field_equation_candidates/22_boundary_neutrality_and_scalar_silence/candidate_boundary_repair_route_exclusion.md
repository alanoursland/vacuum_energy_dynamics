# Candidate Boundary Repair Route Exclusion

## Canonical Filename

```text
candidate_boundary_repair_route_exclusion.md
```

This document summarizes the output of:

```text
candidate_boundary_repair_route_exclusion.py
```

## What This Document Is

This document is the fifth artifact for `22_boundary_neutrality_and_scalar_silence/`.

It is not a boundary theorem, not a scalar-silence proof, not a compact-support theorem, not a neutral-transport theorem, and not a parent field equation.

Its purpose is to inventory and reject boundary/scalar/current repair mechanisms.

The locked-door question was:

```text
Which boundary repair routes must remain rejected?
```

The result is:

```text
Boundary/scalar silence cannot be supplied by repair routes.

Surface counterterms, boundary repair currents, R_V cancellation,
J_exch repair, curvature boundary rescue, H boundary counterterms,
O boundary erasure, dark boundary patches, recovery-tuned smoothing,
and sharp support hiding shell charge remain rejected.

Only inert diagnostics and future derived no-repair theorems remain possible.
```

Tiny goblin label:

```text
No patch purse.
No seam spell.
No repair goblin.
```

---

## Archive Dependency Status

The run reported a clean archive dependency check:

```text
boundary_scalar_silence_target_ledger_dependency_22: dependency_satisfied
smooth_compact_no_shell_dependency_22: dependency_satisfied
sector_scalar_silence_dependency_22: dependency_satisfied
boundary_current_flux_silence_dependency_22: dependency_satisfied
```

So the repair-exclusion audit was connected to the target ledger, smooth compact support / no-shell audit, scalar-tail sector audit, and boundary-current flux silence audit.

---

## Reduced Repair Diagnostics

The script recorded that repair terms still carry the same reduced witnesses.

For repair A-tail terms:

\[
q_{\rm total,repair}=q_{\rm counter}+q_{\rm shell}+q_{\rm smoothing},
\]

the mass shift is:

\[
\delta M_A|_{\rm repair}
=
-\frac{c^2}{2G}
(q_{\rm counter}+q_{\rm shell}+q_{\rm smoothing}).
\]

For scalar-tail repair:

\[
F_{\rm scalar}|_{\rm repair}=-4\pi C_{\rm tail}.
\]

For repair current flux:

\[
\Phi_{\rm current}|_{\rm repair}=I_{\rm repair}.
\]

Neutral repair would therefore require those coefficients to vanish or be independently theorem-routed. Choosing repair coefficients after leakage appears is not a theorem.

---

## Rejected Boundary Repair Routes

The script rejected ten repair routes.

| Entry | Route | Status | Consequence |
|---|---|---|---|
| R1 | surface counterterm | REJECTED | no surface counterterm may supply boundary neutrality by repair |
| R2 | boundary repair current | REJECTED | boundary currents must vanish or be future theorem-routed as neutral transport |
| R3 | \(R_V\) boundary cancellation | REJECTED | \(R_V\) cannot be a boundary purse |
| R4 | \(J_{\rm exch}\) repair | REJECTED | \(J_{\rm exch}\) remains role-level / theorem-targeted |
| R5 | curvature boundary rescue | REJECTED | curvature cannot rescue boundary neutrality |
| R6 | H boundary counterterm | REJECTED | H remains diagnostic-only audit label |
| R7 | O boundary eraser | REJECTED | O remains theorem-targeted; diagnostic-only labels are safe only if inert |
| R8 | dark boundary patch | REJECTED | dark labels cannot make ordinary boundary/scalar silence true |
| R9 | recovery-tuned smoothing | REJECTED | boundary conditions cannot be selected by recovery target |
| R10 | sharp support hiding shell charge | REJECTED | sharp support is not scalar silence |

---

## Allowed Non-Repair Routes

The script preserved only inert diagnostics and future theorem targets:

```text
diagnostic boundary audit,
smooth matched profile diagnostic,
derived no-shell support,
neutral transport theorem.
```

These are not repairs.

They are safe only if they do not modify equations, sources, metrics, coefficients, boundary data, recovery behavior, or mass routing by declaration.

---

## What This Study Established

This study established that boundary/scalar silence cannot be supplied by:

```text
surface counterterms,
boundary repair currents,
R_V boundary cancellation,
J_exch repair,
curvature boundary rescue,
H boundary counterterms,
O boundary erasers,
dark boundary patches,
recovery-tuned smoothing,
sharp support hiding shell charge.
```

It also restated the positive future routes:

```text
derived no-shell matching,
derived compact support,
derived neutral transport,
inert diagnostic boundary audit.
```

---

## What This Study Did Not Establish

This study did not prove:

```text
boundary neutrality,
scalar silence,
compact support,
no-shell matching,
neutral transport,
residual-kill,
active O,
H insertability,
dark-sector coupling,
curvature-current neutrality,
exchange-current neutrality,
parent field equation readiness.
```

It only rejects repair routes.

---

## Failure Controls

The boundary repair route exclusion audit fails if later scripts allow:

1. Surface counterterm as boundary neutrality.
2. Boundary repair current introduced after leakage.
3. \(R_V\) to erase boundary mismatch.
4. \(J_{\rm exch}\) to repair scalar/source/boundary failure.
5. Curvature diagnostic/current/balance to rescue boundary behavior.
6. \(H_{\rm curv}/H_{\rm exch}\) as boundary counterterm or \(M_{\rm ext}\) correction.
7. O to erase boundary/scalar leakage by name.
8. Dark label to patch ordinary boundary failure.
9. Recovery target to select smoothing/support/boundary data.
10. Sharp support to hide shell charge.
11. Repair coefficients to be chosen after leakage appears.

---

## Next Development Target

The next script should be:

```text
candidate_diagnostic_residual_nonmetric_conditions.py
```

Purpose:

```text
Clarify when a residual may survive as diagnostic/non-metric without affecting mass, scalar silence, source routing, or boundary behavior.
```

Expected role:

```text
requirements / diagnostic-residual audit;
not a residual-kill theorem.
```
