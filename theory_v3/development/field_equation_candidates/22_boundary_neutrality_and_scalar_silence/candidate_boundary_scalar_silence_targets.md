# Candidate Boundary Scalar Silence Targets

## Canonical Filename

```text
candidate_boundary_scalar_silence_targets.md
```

This document summarizes the output of:

```text
candidate_boundary_scalar_silence_targets.py
```

## What This Document Is

This document is the opening artifact for `22_boundary_neutrality_and_scalar_silence/`.

It is not a boundary theorem, not a scalar-silence proof, not a parent field equation, not a no-overlap operator, not a correction-tensor insertion, and not a shell-source law.

Its purpose is to make the Group 22 boundary-neutrality and exterior scalar-silence targets explicit.

The locked-door question was:

```text
What exactly must vanish for boundary neutrality and exterior scalar silence?
```

The result is:

```text
Group 22 starts from explicit vanishing/no-repair targets.

The targets are required but not derived.

Boundary/scalar silence remains theorem-targeted.
```

Tiny goblin label:

```text
No boundary purse.
No tail ghost.
Show the empty edge.
```

---

## Archive Dependency Status

The run reported a clean archive dependency check:

```text
group21_status_summary_dependency_22: dependency_satisfied
A_sector_mass_definition_dependency_22: dependency_satisfied
residual_scalar_tail_flux_dependency_22: dependency_satisfied
boundary_tail_delta_A_flux_dependency_22: dependency_satisfied
source_routing_no_double_counting_dependency_22: dependency_satisfied
```

So the Group 22 opening script was correctly connected to the Group 21 status summary, A-sector mass definition, scalar-tail flux audit, boundary A-tail flux audit, and source-routing no-double-counting audit.

---

## Reduced Danger Witnesses

The script re-stated the three reduced danger witnesses inherited from Group 21.

### Residual scalar tail

For:

\[
\phi_{\rm tail}(r)=\frac{C}{r},
\]

the reduced surface flux is:

\[
F_\phi=4\pi r^2\phi_{\rm tail}'=-4\pi C.
\]

Therefore a residual ordinary-sector \(1/r\) scalar tail is not silent unless \(C=0\), or unless it is strictly non-metric/diagnostic, or unless a future parent theorem routes it without double counting.

### Boundary A-tail

For:

\[
\delta A_{\rm boundary}(r)=\frac{q}{r},
\]

the A-flux shift is:

\[
\delta F_A=-4\pi q.
\]

The A-sector mass shift is:

\[
\delta M_A=-\frac{c^2q}{2G}.
\]

Therefore non-A boundary behavior must not leave a nonzero exterior A-tail.

### Far-zone radial current

For:

\[
j^r=\frac{I}{4\pi r^2},
\]

the sphere flux is:

\[
\Phi=4\pi r^2 j^r=I.
\]

Therefore non-A far-zone currents are silent only when \(I=0\), unless a future current theorem derives a neutral transport interpretation.

---

## Boundary / Scalar Silence Target Ledger

| Entry | Target | Status | Consequence |
|---|---|---|---|
| S1 | \(\delta F_A|_{\rm boundary,non-A}=0\) | REQUIRED | boundary mass preservation remains theorem-targeted |
| S2 | \(C_\zeta=0\) | REQUIRED | zeta residual must be killed, non-metric, compact-neutral, or theorem-routed |
| S3 | \(C_\kappa=0\) | REQUIRED | kappa remains diagnostic/suppressed/non-metric unless derived otherwise |
| S4 | \(C_{JV}=0\) | REQUIRED | unresolved \(J_V\) must not leave a hidden scalar charge |
| S5 | \(C_{\rm curv}=0\) | REQUIRED | curvature remains diagnostic/accounting/branch-filter unless source-neutral dynamics are derived |
| S6 | \(C_H=0\) | REQUIRED | \(H_{\rm curv}/H_{\rm exch}\) remain non-insertable unless trace neutrality is derived |
| S7 | \(I_{\rm nonA}=0\) | REQUIRED | non-A far-zone currents must vanish or be theorem-routed as neutral transport |
| S8 | no shell source | REQUIRED | compact support is unsafe unless matching/no-shell conditions are derived |
| S9 | no recovery-tuned smoothing | REQUIRED | recovery remains downstream diagnostic, not construction rule |
| S10 | no active \(O\) | REQUIRED | no-overlap remains theorem-targeted; diagnostic labels only are safe |
| S11 | no \(H\) insertion | REQUIRED | H labels remain diagnostic audit placeholders only |

These entries are requirements and theorem targets, not solved claims.

---

## Forbidden Boundary / Scalar-Silence Shortcuts

The script rejected:

```text
smoothing repair,
O scalar-tail eraser,
H boundary counterterm,
hidden shell source,
recovery-tuned boundary condition,
dark boundary patch.
```

These are governance exclusions.

They do not prove boundary neutrality; they prevent fake boundary neutrality.

---

## Theorem Obligations Opened

The script opened the following theorem obligations:

```text
derive no-shell boundary condition,
derive residual scalar silence,
derive non-A boundary A-flux neutrality,
derive far-zone non-A current flux silence,
derive compact-support matching law,
derive diagnostic residual non-reentry,
derive no recovery-tuned boundary data,
derive source-routing compatibility with boundary/scalar silence.
```

These obligations are prerequisites for claiming boundary neutrality and exterior scalar silence.

---

## What This Study Established

This study established the Group 22 target ledger.

The required targets are:

```text
delta F_A|boundary,non-A = 0
C_zeta = 0
C_kappa = 0
C_JV = 0
C_curv = 0
C_H = 0
I_nonA = 0
no shell source
no recovery-tuned smoothing
no active O
no H insertion
```

It also preserved the reduced witnesses:

```text
phi_tail = C/r -> F_phi = -4*pi*C
delta_A_boundary = q/r -> delta_M_A = -c^2 q/(2G)
j^r = I/(4*pi*r^2) -> Phi = I
```

---

## What This Study Did Not Establish

This study did not prove:

```text
boundary neutrality,
scalar silence,
compact support,
no-shell matching,
residual-kill,
active no-overlap,
H insertability,
J_V boundary-current neutrality,
source-routing compatibility,
parent field equation readiness.
```

It only made the target conditions explicit.

---

## Failure Controls

The boundary/scalar silence target audit fails if a later script allows:

1. Boundary neutrality by assumption.
2. Scalar silence by assumption.
3. \(O\) as scalar-tail eraser.
4. \(H\) as boundary counterterm.
5. Smoothing chosen after recovery failure.
6. Shell source hidden by derivative jump.
7. Compact support without matching law.
8. Residual-kill treated as derived no-overlap.
9. Dark label boundary patch.
10. Curvature or exchange boundary repair.
11. \(J_V\) boundary current without definition.
12. Source cancellation across non-A sectors.
13. Recovery-selected boundary condition.

---

## Next Development Target

The next script should be:

```text
candidate_smooth_compact_support_no_shell_conditions.py
```

Purpose:

```text
Test when compact support avoids hiding a shell source.
```

Expected role:

```text
diagnostic / requirements audit;
not a compact-support theorem.
```
