# Candidate Route Branch Requirement Matrix

## Purpose

This script classified route types by whether they require an active `B_s` branch, tolerate split notation, or can only use neutral `F_zeta` deferral.

## Result

The matrix separated branch-required, split-safe, neutral-safe, conditional, and not-ready routes.

It made no branch choice, installed no declaration, adopted nothing, proved nothing, and opened no downstream route.

## Route classifications

- Trace-normalization declaration completion is branch-required.
- Safe-membership declaration completion is conditional: some membership fields may be inventoried, but active Package B use requires branch visibility.
- Notation-quality source hierarchy is split-safe.
- Neutral `F_zeta` bookkeeping is neutral-safe only while expression-free.
- Explicit adoption requires declaration clarity or explicit unresolved-branch risk.
- Theorem routes after declarations require an active branch and explicit assumptions.
- Insertion and parent routes remain not ready even if a branch is later chosen.

## Rejected upgrades

The script rejected branch-independent normalization, membership licensing branch choice, branch choice licensing downstream use, and adoption under hidden branch.

## Open obligations

Future handoffs must preserve route requirements, keep neutral `F_zeta` honest, and keep insertion and parent closure not ready.

## Safe handoff

The next script is:

```text
candidate_split_notation_safe_continuation_sieve.py
```
