# candidate_forbidden_use_fence — Result Note

## Result

The script records the hard forbidden-use fence for diagnostic-only status.

Forbidden:

```text
field-equation insertion;
stress tensor claim;
ordinary-source response;
mass response;
trace response;
covariant conservation claim;
parent equation ingredient;
active-O patch;
Package B adoption evidence.
```

## Main Findings

This is the enforcement half of the downgrade.

The diagnostic object cannot be used as a field-equation term, stress tensor, source response, mass response, trace response, covariant conservation proof, parent ingredient, active-O patch, or Package B adoption evidence.

This closes the common leak paths. The diagnostic object may remain useful as a clue, but it cannot carry physical load.

Rejected shortcuts:

```text
term insertion;
stress tensor by wording;
active-O patch.
```

## Boundary

The script does not erase the diagnostic ledger. It only forbids physical upgrades.

## Steering Consequence

Proceed to status conversion. The ledger and fence together support assigning formal `DIAGNOSTIC_ONLY` status.
