# candidate_moment_hierarchy_problem — Analysis Note

## Result

`candidate_moment_hierarchy_problem.py` opens Group 87 as a finite moment-hierarchy test.

The central question is:

```text
Is the Group 86 quartic profile part of a systematic hierarchy?
```

The imported Group 86 state is correct:

```text
moment map derived;
degree 4 minimal for M2/M4 suppression;
quartic profile unique and zero-action minimizing;
weighted suppression W0..W3 follows from M0..M5;
local rho and higher moments remain;
physical/covariant origin open;
parent divergence identity unproven;
recombination blocked.
```

The planned route is to solve normalized even profiles of degree `2N` for:

```text
N = 1..4
```

## Interpretation

This opener chooses the right target.

Group 86 made the quartic profile look structurally meaningful inside the reduced model. But a single structural profile can still be a one-off. Group 87 asks a sharper question:

```text
Is quartic suppression the N=2 case of a broader finite-order suppression hierarchy?
```

That is a real progress question because a hierarchy changes the theory map. A one-off profile is a useful local trick. A hierarchy suggests a systematic payload-suppression mechanism.

## What Changed

The project moves from:

```text
derive the quartic profile
```

to:

```text
test whether profile order controls the number of suppressed moments
```

That is the correct next level.

## What Did Not Change

The opener correctly prevents overclaiming:

```text
finite hierarchy evidence is not all-order proof;
parent divergence remains unproven;
recombination remains blocked.
```

That scope is important because `N=1..4` can show pattern, not infinity.

## Steering Consequence

The group should be judged by three things:

```text
1. Are profiles found for N=1..4?
2. Are they unique?
3. Does each profile leave a next obstruction?
```

The results answer all three.
