# candidate_boundary_null_profile — Result Note

## Result

The script constructs a nontrivial reduced boundary-null profile:

```text
W(r) = r^2*(R-r)^2
```

and verifies:

```text
W(R) = 0
W'(R) = 0
W(0) = 0
```

## Main Findings

This is genuine reduced constructive progress. It shows that a nontrivial internal radial profile can be built with no boundary value and no first-derivative boundary leak.

That matters for the silent/inert route because a boundary-null profile can support:

```text
no exterior boundary value leakage;
no first-derivative shell jump by itself;
regular center behavior in this reduced model.
```

But the script correctly avoids overclaiming. A boundary-null profile alone does not imply zero scalar charge, source safety, divergence silence, or a covariant insertion law.

## Boundary

This is a reduced radial profile, not a full covariant construction. It is not an insertion law and not a physical-use license.

## Steering Consequence

The next required check is charge neutrality. Boundary-null behavior does not automatically mean zero exterior scalar charge.
