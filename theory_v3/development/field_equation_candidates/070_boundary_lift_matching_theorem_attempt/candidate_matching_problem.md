# candidate_matching_problem — Result Note

## Result

The opener correctly frames Group 70 as a boundary-lift matching theorem attempt.

Central question:

```text
Can L_boundary = -(D_jump + D_layer + D_tail) be derived from common geometry?
```

The script preserves the correct hard fences:

```text
parent equation remains blocked;
diagnostic transition remains non-insertable;
chosen matching is repair-like;
active O remains unconstructed.
```

## Main Findings

Group 70 is aimed at the retained Group 69 structural route:

```text
L_boundary = -(D_jump + D_layer + D_tail)
```

The opener correctly prevents the common bad moves:

```text
choosing L_boundary to cancel the boundary sum;
writing a parent equation during the theorem attempt;
escaping to active O by label.
```

This is the right starting point. The group is not trying to build the parent equation. It is trying to determine whether the boundary/lift matching relation can be derived rather than selected.

## Boundary

No matching theorem is proved in this opener. No parent divergence identity is proved. No recombination is opened.

## Steering Consequence

Proceed to the common-generator ansatz. The next step should test whether a shared boundary generator can reduce the matching problem to orientation, coefficient, and bulk/gauge burdens.
