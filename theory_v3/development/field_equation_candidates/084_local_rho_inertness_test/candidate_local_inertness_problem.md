# candidate_local_inertness_problem — Analysis Note

## Result

`candidate_local_inertness_problem.py` opens Group 84 as a finite-mode local inertness test for the Group 83 skewed local `rho`.

The imported state is the right one:

```text
weighted skew c = 3ell/(2R) derived in reduced class;
weighted exactness route strengthened;
local rho nonzero remains;
payload inertness remains open;
full covariant theorem remains open;
parent divergence identity unproven;
recombination blocked.
```

The group boundary is explicit:

```text
finite-mode inertness test only;
not a full payload theorem.
```

## Interpretation

This opener correctly targets the biggest remaining physical risk after Groups 82–83.

Groups 82–83 made exactness genuinely useful:

```text
flat total neutrality derived;
weighted total neutrality restored by a geometrically derived skew.
```

But neither result showed that local nonzero `rho` is harmless. Group 84 asks the right next question:

```text
Does the locally nonzero rho couple to simple payload probes?
```

That is a real physics-facing test, not bookkeeping.

## What Changed

The project focus moves from global/weighted charge cancellation to local detectability.

The status before this group was:

```text
payload inertness open.
```

The intended status after this group should be sharper:

```text
payload inertness supported;
payload inertness obstructed;
or payload inertness unresolved under a concrete finite-mode test.
```

## What Did Not Change

The opener correctly refuses to overclaim. A finite probe basis cannot prove full physical inertness. It can only detect or fail to detect low-order local payload.

That matters because a positive finite-mode result would still be limited, while a negative result is highly informative: if even `1`, `y`, or `y^2` see `rho`, full inertness is unlikely without extra structure.

## Steering Consequence

The next file must define the probe basis. The useful test is not whether total charge vanishes again, but whether low-order local moments vanish.
