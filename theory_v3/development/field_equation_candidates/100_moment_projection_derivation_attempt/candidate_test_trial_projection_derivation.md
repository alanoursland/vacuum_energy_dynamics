# candidate_test_trial_projection_derivation — Analysis Note

## Result

`candidate_test_trial_projection_derivation.py` verifies the exact projection representation:

```text
A[k,j] = 2 ∫_0^1 psi_k(x) phi_j(x) (1-x^2)^4 dx
```

where:

```text
phi_j(x) = x^(2j)

psi_k(x) = x^(2k) - ((2k-1)/(2k+3)) x^(2k-2).
```

The script checks `k,j = 1..7` and reports:

```text
projection equality failures: []
```

Governance records:

```text
formal weighted projection:
  PASS

test functions:
  PASS

trial functions:
  PASS

physical residual:
  OBLIGATION, not derived.
```

## Interpretation

This is the core success of Group 100.

Group 099 found that the moment sequence had a weighted integral form. Group 100 now shows that the full hierarchy matrix entries are generated by an explicit test/trial projection.

That upgrades the status from:

```text
moment/projection origin plausible but underderived
```

to:

```text
formal weighted projection derived.
```

This is real progress.

## What It Does Not Prove

The result is formal, not physical. It does not tell us what residual equation is being projected. It does not identify the source vector, boundary conditions, or physical ledger.

It also blocks a naive Hessian interpretation because this is a test/trial pairing, not automatically a symmetric Gram/Hessian construction.

## Carry-forward status

```text
FORMAL_WEIGHTED_PROJECTION_DERIVED
TEST_FUNCTIONS_IDENTIFIED
TRIAL_FUNCTIONS_IDENTIFIED
A_KJ_PROJECTION_EQUALITY_VERIFIED_N1_TO_N7
PHYSICAL_RESIDUAL_NOT_DERIVED
SOURCE_VECTOR_NOT_DERIVED
BOUNDARY_CONDITIONS_NOT_DERIVED
NAIVE_HESSIAN_INTERPRETATION_NOT_LICENSED
```
