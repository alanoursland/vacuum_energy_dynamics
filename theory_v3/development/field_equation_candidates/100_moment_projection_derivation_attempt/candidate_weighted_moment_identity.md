# candidate_weighted_moment_identity — Analysis Note

## Result

`candidate_weighted_moment_identity.py` verifies the weighted moment identity:

```text
beta_moment(s) = 2 ∫_0^1 x^(2s)(1-x^2)^4 dx.
```

The computed integral is:

```text
∫_0^1 x^(2s)(1-x^2)^4 dx
=
384 / (32s^5 + 400s^4 + 1840s^3 + 3800s^2 + 3378s + 945).
```

The target moment is:

```text
beta_moment(s)
=
768 / [(2s+1)(2s+3)(2s+5)(2s+7)(2s+9)].
```

Exact sample checks for `n=1..10` report:

```text
identity failures: []
```

## Interpretation

This confirms the Group 099 clue locally in Group 100.

The moment sequence is not arbitrary. It is generated by an explicit positive weight:

```text
w(x) = (1-x^2)^4
```

on the unit interval, with even powers.

That is a meaningful formal origin clue.

## What It Does Not Prove

This does not derive dynamics. It does not identify a source vector, boundary conditions, physical domain, or burden functional.

## Carry-forward status

```text
WEIGHTED_MOMENT_IDENTITY_VERIFIED_N1_TO_N10
WEIGHT_FUNCTION_IDENTIFIED
BETA_MOMENT_FORMAL_ORIGIN_SUPPORTED
MOMENT_IDENTITY_NOT_FIELD_EQUATION
PHYSICAL_SOURCE_NOT_DERIVED
```
