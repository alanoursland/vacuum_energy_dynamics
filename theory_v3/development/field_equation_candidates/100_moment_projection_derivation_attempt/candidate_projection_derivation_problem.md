# candidate_projection_derivation_problem — Analysis Note

## Result

`candidate_projection_derivation_problem.py` opens Group 100 as a formal weighted projection derivation attempt.

The target is:

```text
A[k,j] = 2 ∫ psi_k phi_j (1-x^2)^4 dx
```

with:

```text
phi_j = x^(2j)

psi_k = x^(2k) - ((2k-1)/(2k+3)) x^(2k-2).
```

The governance section correctly frames the scope:

```text
formal projection origin,
not physical residual/source derivation.
```

It also blocks overclaims:

```text
projection as field equation:
  rejected because residual/source/boundary are not derived

projection as burden ledger:
  rejected because physical ledger assignment remains deferred.
```

## Interpretation

This opener is exactly the right continuation from Group 099.

Group 099 showed that the hierarchy had a weighted moment scent. Group 100 turns that into a concrete test/trial projection target.

The important distinction is:

```text
formal projection derivation
  !=
physical field equation derivation.
```

The group is allowed to show how `A_N` is mathematically generated. It is not allowed to say what physical residual or burden law generated it.

## Carry-forward status

```text
FORMAL_PROJECTION_DERIVATION_ATTEMPT_OPENED
WEIGHT_TEST_TRIAL_TARGET_DEFINED
PROJECTION_AS_FIELD_EQUATION_BLOCKED
PROJECTION_AS_BURDEN_LEDGER_BLOCKED
PHYSICAL_RESIDUAL_SOURCE_BOUNDARY_OPEN
```
