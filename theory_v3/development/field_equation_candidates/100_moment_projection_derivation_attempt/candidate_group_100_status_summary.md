# candidate_group_100_status_summary — Analysis Note

## Result

`candidate_group_100_status_summary.py` closes Group 100 with:

```text
weight function identified: w(x)=(1-x^2)^4;
trial functions identified: phi_j=x^(2j);
test functions identified: psi_k=x^(2k)-((2k-1)/(2k+3))x^(2k-2);
formal projection derived: A[k,j]=2∫psi_k phi_j w dx;
row test functions are sign-changing with interior roots;
physical residual/source/boundary not derived;
physical ledger assignment deferred;
hierarchy remains auxiliary admissibility candidate;
parent equation not ready;
recombination blocked.
```

Recommended next routes are:

```text
101_residual_source_reconstruction_attempt
101_difference_numerator_factorization_attempt
101_burden_functional_minimum_requirements
```

## Interpretation

This summary is correct.

Group 100 is a real progress group. It derives the formal projection machinery behind `A_N`. The hierarchy is no longer merely “moment-like”; it is explicitly generated by weighted test/trial projection.

But the status remains carefully bounded:

```text
formal origin:
  derived

physical origin:
  not derived
```

## Carry-forward status

```text
GROUP_100_FORMAL_PROJECTION_DERIVATION_COMPLETE
WEIGHT_FUNCTION_IDENTIFIED
TEST_FUNCTIONS_IDENTIFIED
TRIAL_FUNCTIONS_IDENTIFIED
FORMAL_WEIGHTED_PROJECTION_DERIVED
ROW_TESTS_SIGN_CHANGING
PHYSICAL_RESIDUAL_NOT_DERIVED
SOURCE_VECTOR_NOT_DERIVED
BOUNDARY_CONDITIONS_NOT_DERIVED
PHYSICAL_LEDGER_ASSIGNMENT_DEFERRED
HIERARCHY_REMAINS_AUXILIARY_ADMISSIBILITY_CANDIDATE
PARENT_EQUATION_NOT_READY
RECOMBINATION_BLOCKED
```
