# Candidate Diagnostic vs Active Membership Boundary

## Result

The script stated the diagnostic-versus-active membership boundary and a conservative status ladder.

Active membership is forbidden in Group 41.

## Main Findings

The status ladder is:

```text
diagnostic_label
compatible_if_declared
candidate_if_preconditions_met
future_declaration_candidate
future_theorem_target
active_membership_forbidden_here
```

- Diagnostic membership labels object/sector compatibility only.
- `zeta_Bs -> T_zeta` may remain compatible-if-declared only.
- Membership may become a future candidate only if object, sector, domain/codomain, criterion, role-purity, branch-status, scope, and exclusion obligations close.
- A later declaration route may evaluate safe membership after obligations are satisfied.
- A later theorem route may try to prove membership after declarations and assumptions are explicit.
- Group 41 opens no active Package B membership, active `T_zeta` sector, active `O`, insertion, or parent route.

## Boundary

Diagnostic labels have no source, metric, divergence, projection, insertion, theorem, adoption, or parent effect.

Compatible-if-declared is not declared. A future candidate is not Package B adoption. A future theorem target is not proof. Membership is not residual zero-incidence, residual control, active `O`, or insertion.

## Safe Handoff

Run `candidate_safe_membership_precondition_batch_reconciliation.py` next.
