# candidate_boundary_counterterm_repair_rejection_sieve — Result Note

## Result

The repair sieve rejects fake boundary-silence routes and preserves only the explicit zero-tail / no-shell route.

Rejected routes include:

```text
counterterm cancellation after nonzero flux appears;
hidden shell source;
mass patch by redefining M_ext;
nonzero constant offset treated as silence;
zero charge by fiat.
```

## Main Findings

This script protects Group 54 from a common failure mode: creating a scalar tail or boundary defect and then patching it with a boundary term.

The surviving route is narrow:

```text
zero-tail conditions;
zero scalar charge / zero flux;
no shell jump;
no mass shift.
```

Boundary counterterms do not count as exterior scalar silence unless they arise from a legitimate theorem rather than a repair.

The script also keeps conditional route survival distinct from insertion permission.

## Boundary

This is a rejection sieve, not a proof of neutrality. It does not show that zero charge or no-shell matching holds.

## Steering Consequence

The group summary should explicitly reject boundary repair as a safety theorem. The only surviving reduced route is a clean zero-tail / no-shell route with physical use still blocked pending theorem support.
