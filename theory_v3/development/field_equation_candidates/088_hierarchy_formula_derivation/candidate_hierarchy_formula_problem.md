# candidate_hierarchy_formula_problem — Analysis Note

## Result

`candidate_hierarchy_formula_problem.py` opens Group 88 as a formula-derivation group.

The imported Group 87 state is:

```text
finite hierarchy supported through N=4;
unique profile per order through N=4;
weighted block inheritance derived;
next moment obstruction persists;
all-order closure not proven;
local rho nonzero remains;
physical/covariant origin open;
parent divergence identity unproven;
recombination blocked.
```

The group target is:

```text
derive moment-ratio identity and Beta-function coefficient system.
```

## Interpretation

This opener chooses the right next target.

Group 87 already showed a finite pattern through `N=4`. Extending to `N=5` and `N=6` alone would have been weaker progress. Group 88 instead asks for the rule that generates the hierarchy.

That is the correct escalation:

```text
Group 87:
  finite profiles exist.

Group 88:
  derive the coefficient generator.
```

This matters because a generator formula changes the status of the hierarchy from observed pattern to structured construction.

## What Changed

The project is no longer trying to infer hierarchy from isolated solved profiles. It is trying to derive the algebraic machine behind those profiles.

That machine, if successful, should explain:

```text
why the moment constraints are linear;
why Beta functions appear;
how each finite profile is generated;
what remains open for all-order closure.
```

## What Did Not Change

The opener correctly keeps the scope tight:

```text
finite-N formula is not all-order closure;
all-order determinant nonzero is not proven;
parent divergence and recombination remain blocked.
```

## Steering Consequence

The group should be judged by whether it derives a usable finite-N coefficient formula. The results show it does: the moment-ratio identity, Beta linear system, and Cramer formula are all derived and validated.
