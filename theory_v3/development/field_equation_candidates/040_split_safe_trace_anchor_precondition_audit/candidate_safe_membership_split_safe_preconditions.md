# Candidate Safe-Membership Split-Safe Preconditions

## Result

The script inventories safe-membership preconditions that can be audited while the active `B_s` branch remains unchosen.

## Main Findings

- The `T_zeta` sector criteria and role-purity exclusions are split-safe audit objects.
- The `zeta_Bs` object slot, domain/codomain, and membership criterion are conditional and must remain visible.
- Active Package B membership remains branch-required and is not installed.
- Membership cannot choose the branch, prove itself, supply source no-double-counting, or act as an active projector.

## Boundary

Safe membership remains compatible-if-declared only. The script does not install membership, does not prove incidence, does not kill residuals, and does not open insertion.

## Safe Handoff

Run `candidate_residual_source_safety_split_audit.py` next.
