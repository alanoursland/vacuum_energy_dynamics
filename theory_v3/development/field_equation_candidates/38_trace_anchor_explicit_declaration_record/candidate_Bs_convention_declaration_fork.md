# Candidate B_s Convention Declaration Fork

## Script Result

The script explored the central B_s convention fork. It compared the scale-factor and metric-coefficient conventions, but the configured choice was:

```text
CHOSEN_CONVENTION = None
```

So no B_s convention was installed.

## Convention Branches

Two coherent branches remain declaration-ready options:

```text
scale-factor convention:
  B_s is a scale-factor response.
  log(B_s) = zeta / d.

metric-coefficient convention:
  B_s is metric-coefficient language.
  log(B_s) = 2 zeta / d.
```

The no-choice branch is a valid conservative outcome:

```text
DECLARATION_DEFERRED
```

The both-chosen branch is invalid unless separate named notations are introduced.

## Rejected Upgrades

The script rejects:

```text
hidden factor-of-two conventions,
recovery-selected convention,
convention as adoption,
convention as insertion.
```

## Final Status

```text
B_s convention choice remains deferred.
No convention is selected.
No declaration value is installed.
No adoption, theorem proof, insertion, active O, residual control, or parent closure is licensed.
```
