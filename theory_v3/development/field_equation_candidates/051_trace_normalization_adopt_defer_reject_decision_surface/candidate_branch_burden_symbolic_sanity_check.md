# candidate_branch_burden_symbolic_sanity_check — Result Note

## Result

The symbolic sanity check confirms that the paired trace-normalization expressions remain genuinely distinct.

The script computes:

```text
(2*zeta/d) - (zeta/d) = zeta/d
```

and verifies the residual against the expected difference is zero. The factor-of-two burden is therefore still live.

## Main Findings

This is the one algebraic anchor in Group 51. It shows that the metric-side candidate,

```text
log(B_s_metric)=2*zeta/d
```

and the scale-side candidate,

```text
log(b_s_scale)=zeta/d
```

are not the same expression unless an additional convention, branch choice, or collapse rule is supplied.

That matters because it blocks adoption-by-summary. A later document cannot honestly treat the paired record as one neutral law while the symbolic difference remains `zeta/d`.

The governance interpretation is correctly limited. The derivation supports only the claim that the branch burden remains live. It does not decide adoption, does not choose a branch, does not provide physical-use support, and does not prove residual/source/boundary safety.

## Boundary

The check is not adoption evidence. It is not a selector. It is not a neutralization rule. It is not an insertion theorem.

## Steering Consequence

Any decision-surface summary must keep both expressions visible. If a future route wants to collapse or choose between them, it needs a separate daylight-labeled convention or branch decision. The algebra does not erase the burden.
