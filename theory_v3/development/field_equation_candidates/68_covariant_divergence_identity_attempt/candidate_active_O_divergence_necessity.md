# candidate_active_O_divergence_necessity — Result Note

## Result

The script classifies active `O` with respect to the remaining divergence burden.

Preferred O-free identity:

```text
D_boundary + D_lift = 0
```

Conditional O-required identity:

```text
D_O = -D_boundary - D_lift
```

Rejected route:

```text
O by label
```

## Main Findings

Active `O` is not licensed by the mere need for cancellation.

The preferred theorem target remains O-free:

```text
D_lift + D_boundary = 0
```

The O-required route is only meaningful if `O` is actually constructed and is not just another repair current.

The script correctly rejects:

```text
O as repair;
O-first because divergence is hard;
pretending O status is solved.
```

## Boundary

No active `O` is constructed. No O-mediated divergence identity is proved.

## Steering Consequence

Proceed to the route classifier. The classifier should preserve the sharpened identity target while keeping divergence identity and recombination blocked.
