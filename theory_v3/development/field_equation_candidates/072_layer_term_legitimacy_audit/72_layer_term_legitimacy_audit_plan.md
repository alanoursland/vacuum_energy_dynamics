# 72_layer_term_legitimacy_audit — Plan

## Purpose

Group 72 audits the sharpest boundary-side blocker left by Group 71:

```text
D_layer legitimacy
```

Group 71 did not establish a strong common boundary generator in its tested classes. It retained the boundary-lift matching route only as a partial theorem target / controlled obstruction and split the remaining work into:

```text
orientation/component anti-match theorem;
D_layer legitimacy;
L_bulk / L_gauge lift-cleanliness obligations.
```

Group 72 now asks whether `D_layer` can legitimately appear as a boundary component of the common generator, or whether it is just the old diagnostic transition response trying to re-enter as a physical divergence term.

This is not a parent-equation construction group.

## Group Name

```text
72_layer_term_legitimacy_audit
```

## Central Question

```text
Can D_layer be a legitimate boundary-generator component, or must it remain diagnostic / unresolved / rejected as a hidden transition insertion?
```

## Starting State

Imported from Group 71:

```text
STRONG_GENERATOR_NOT_ESTABLISHED_IN_TESTED_CLASSES;
CONTROLLED_OBSTRUCTION;
SPLIT_THEOREM_TARGETS_REQUIRED;
orientation anti-match remains compatibility unless derived;
component anti-match remains compatibility unless derived;
D_layer legitimacy remains unresolved;
L_bulk = 0 and L_gauge = 0 remain lift-cleanliness obligations;
parent divergence identity remains unproven;
recombination remains blocked.
```

## Desired Outcome

A useful result would be one of these:

### Route A: Layer Component Retained as Theorem Target

```text
D_layer is not supplied by the old diagnostic transition response.
A legal layer-component criterion is stated.
A boundary-geometric layer component remains possible, but not derived.
Status:
  D_LAYER_LEGITIMACY_NOT_ESTABLISHED;
  BOUNDARY_LAYER_COMPONENT_RETAINED_AS_THEOREM_TARGET.
```

### Route B: Diagnostic Layer Rejected

```text
Any route that uses the old diagnostic transition response as D_layer is rejected.
Any route that carries ordinary source, trace payload, mass shift, or repair current through D_layer is rejected.
Status:
  DIAGNOSTIC_TRANSITION_INSERTION_REJECTED.
```

### Route C: Layer Route Downgraded

```text
If all tested layer classes either use diagnostic transition material, repair current, source/trace payload, or hidden coefficients, then D_layer is not a legitimate component in the tested classes.
Status:
  D_LAYER_NOT_ESTABLISHED_IN_TESTED_CLASSES;
  BOUNDARY_LIFT_ROUTE_SPLIT_REQUIRED.
```

Do not promote this to a no-go theorem unless the tested classes are broad enough to justify it.

## What This Group May Do

Group 72 may:

```text
define legal D_layer criteria;
reject diagnostic transition insertion;
test layer-as-boundary-component algebra;
test endpoint/locality/measure criteria diagnostically;
test source/trace/divergence payload filters;
classify D_layer as retained theorem target, unresolved blocker, or rejected tested route.
```

## What This Group Must Not Do

Group 72 must not:

```text
write a parent field equation;
insert B_s/F_zeta;
revive or insert the old diagnostic transition response;
use eta, eta^2, N_w, R1/R2, or smoothstep residues as physical D_layer terms;
construct active O by label;
choose a_layer=-1 by convenience;
claim boundary-lift matching theorem proven;
claim D_layer legitimacy proven unless derived from boundary geometry;
claim source safety, trace safety, mass neutrality, divergence identity, or boundary neutrality;
open recombination.
```

## Recommended Script Batch

```text
candidate_layer_legitimacy_problem.py
candidate_layer_component_requirements.py
candidate_diagnostic_transition_exclusion.py
candidate_layer_as_boundary_component_test.py
candidate_layer_measure_support_test.py
candidate_layer_source_trace_divergence_filter.py
candidate_layer_legitimacy_route_classifier.py
candidate_group_72_status_summary.py
order.txt
```

## Script Intent

### 1. candidate_layer_legitimacy_problem.py

Open Group 72 as a layer legitimacy audit.

It should restate:

```text
D_layer is the sharpest boundary-side blocker;
old diagnostic transition response is quarantined;
parent equation remains blocked.
```

### 2. candidate_layer_component_requirements.py

State legal requirements for a layer component.

A legitimate `D_layer` must be:

```text
boundary-generated;
local to the boundary/layer region;
not supplied by diagnostic transition response;
not ordinary-source carrying;
not trace carrying;
not a repair current;
not active O;
compatible with the common boundary object.
```

### 3. candidate_diagnostic_transition_exclusion.py

Reject routes where:

```text
D_layer = old diagnostic transition response;
D_layer = R1/R2 repair residue;
D_layer = eta / eta^2 / N_w promoted to physical term;
D_layer = arbitrary counterterm.
```

The old transition work may remain evidence and diagnostic constraint only.

### 4. candidate_layer_as_boundary_component_test.py

Test the algebraic role of `D_layer` as a component:

```text
L_layer = a_layer * D_layer
residual = (a_layer + 1) * D_layer
compatibility requires a_layer = -1.
```

But this is only compatibility. It does not prove `D_layer` legitimacy.

### 5. candidate_layer_measure_support_test.py

Use endpoint/locality/measure diagnostics to test necessary layer criteria.

Endpoint-local profiles may pass diagnostic support criteria, while constant/nonlocal profiles fail. But support/locality does not prove physical layer legitimacy.

### 6. candidate_layer_source_trace_divergence_filter.py

Reject any `D_layer` route that carries:

```text
ordinary source;
trace payload;
mass response;
residual reentry;
repair divergence;
parent-equation effect.
```

Allow only a boundary-divergence component role as future theorem target.

### 7. candidate_layer_legitimacy_route_classifier.py

Classify:

```text
DIAGNOSTIC_TRANSITION_INSERTION_REJECTED;
BOUNDARY_LAYER_COMPONENT_RETAINED_AS_THEOREM_TARGET;
D_LAYER_LEGITIMACY_NOT_ESTABLISHED;
SOURCE_TRACE_PAYLOAD_REJECTED;
PARENT_DIVERGENCE_UNPROVEN;
RECOMBINATION_BLOCKED.
```

### 8. candidate_group_72_status_summary.py

Close Group 72 conservatively.

Expected stable result:

```text
Group 72 rejects diagnostic-transition reentry as D_layer.
It states legal D_layer criteria.
It confirms a_layer=-1 remains compatibility unless boundary geometry derives it.
It does not establish D_layer legitimacy.
The boundary-layer component route remains a theorem target / unresolved blocker.
Parent divergence remains unproven and recombination remains blocked.
```

## Key Success Criteria

A legitimate `D_layer` must be derived, not supplied by an old diagnostic construction.

The goblin wall:

```text
D_layer may be audited as a component.
D_layer may not be supplied by the old diagnostic transition response.
```

## Safe Handoff Options

Likely next groups:

```text
73_layer_generator_construction
73_covariant_lift_neutrality_attempt
73_boundary_lift_route_split_decision
73_active_O_necessity_or_rejection
```

If Group 72 only sharpens the blocker, likely next group:

```text
73_layer_generator_construction
```

If Group 72 strongly rejects layer legitimacy in tested classes:

```text
73_boundary_lift_route_split_decision
```

If layer route remains open but lift terms are higher priority:

```text
73_covariant_lift_neutrality_attempt
```
