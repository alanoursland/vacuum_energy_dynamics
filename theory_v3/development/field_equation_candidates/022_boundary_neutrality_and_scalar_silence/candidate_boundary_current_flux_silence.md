# Candidate Boundary Current Flux Silence

## Canonical Filename

```text
candidate_boundary_current_flux_silence.md
```

This document summarizes the output of:

```text
candidate_boundary_current_flux_silence.py
```

## What This Document Is

This document is the fourth artifact for `22_boundary_neutrality_and_scalar_silence/`.

It is not a current law, not a current-neutrality theorem, not a \(J_V\) definition, not a \(J_{\rm sub}\) or \(J_{\rm exch}\) definition, not a \(J_{\rm curv}\) definition, and not an H insertion theorem.

Its purpose is to audit whether non-A boundary or far-zone currents can remain silent.

The locked-door question was:

```text
Can non-A boundary or far-zone currents remain silent?
```

The result is:

```text
A generic far-zone radial current j_i^r = I_i/(4*pi*r^2) carries sphere flux Phi_i = I_i.

Therefore ordinary exterior current silence requires each non-A current coefficient to vanish,
or the current must remain strictly diagnostic / role-level,
or a future theorem must derive neutral transport with no mass, scalar, boundary, source, or recovery effect.

Total current-flux cancellation is not sector current silence.
```

Tiny goblin label:

```text
No hidden river.
No current purse.
Each flux coin counted.
```

---

## Archive Dependency Status

The run reported a clean archive dependency check:

```text
boundary_scalar_silence_target_ledger_dependency_22: dependency_satisfied
sector_scalar_silence_dependency_22: dependency_satisfied
far_zone_current_flux_witness_dependency_22: dependency_satisfied
```

So the script was correctly connected to the Group 22 target ledger, the scalar-tail sector audit, and the far-zone current-flux witness.

---

## Current-Flux Witness

For a generic radial current:

\[
j^r=\frac{I}{4\pi r^2},
\]

the sphere flux is:

\[
\Phi=4\pi r^2j^r=I.
\]

Thus a nonzero \(I\) is not silent.

Silence requires:

\[
I=0,
\]

unless a future theorem derives a neutral transport interpretation with no ordinary exterior mass, scalar, boundary, source-routing, or recovery effect.

---

## Non-A Current Flux Table

The script applied the same witness to seven non-A current routes:

| Sector | Current Profile | Sphere Flux | Required Condition |
|---|---|---|---|
| \(J_V\) | \(I_V/(4\pi r^2)\) | \(I_V\) | \(I_V=0\) |
| \(J_{\rm sub}\) | \(I_{\rm sub}/(4\pi r^2)\) | \(I_{\rm sub}\) | \(I_{\rm sub}=0\), unless pure-wind neutrality is derived |
| \(J_{\rm exch}\) | \(I_{\rm exch}/(4\pi r^2)\) | \(I_{\rm exch}\) | \(I_{\rm exch}=0\), unless exchange neutrality is derived |
| \(J_{\rm curv}\) | \(I_{\rm curv}/(4\pi r^2)\) | \(I_{\rm curv}\) | \(I_{\rm curv}=0\), unless curvature-current law is derived |
| H flux | \(I_H/(4\pi r^2)\) | \(I_H\) | \(I_H=0\), before any H insertion |
| boundary current | \(I_{\rm boundary}/(4\pi r^2)\) | \(I_{\rm boundary}\) | \(I_{\rm boundary}=0\) |
| dark current | \(I_{\rm dark}/(4\pi r^2)\) | \(I_{\rm dark}\) | \(I_{\rm dark}=0\) in ordinary-sector audit |

---

## Current Flux Silence Ledger

### \(J_V\)

\(J_V\) remains unresolved.

A nonzero far-zone \(I_V\) would make it a second flux coin while undefined.

Required:

```text
I_V = 0
```

or a future theorem deriving neutral transport.

### \(J_{\rm sub}\)

Pure wind is not gravity.

Required:

```text
I_sub = 0
```

unless pure-wind neutrality is derived with:

```text
no M_ext shift,
no scalar trace,
no matter push,
no boundary repair,
no preferred-frame force.
```

### \(J_{\rm exch}\)

Exchange is not repair.

Required:

```text
I_exch = 0
```

unless exchange source/support law derives neutral behavior with no ordinary mass effect.

### \(J_{\rm curv}\)

Curvature current remains undefined.

Required:

```text
I_curv = 0
```

unless a future curvature-current law derives orientation, source side, boundary behavior, and mass neutrality.

### H flux

\(H_{\rm curv}\) and \(H_{\rm exch}\) remain non-insertable.

Required:

```text
I_H = 0
```

before any H insertion.

### Boundary repair current

A boundary current introduced after leakage appears is not boundary neutrality.

Required:

```text
I_boundary = 0
```

### Dark current

Dark sector is not an ordinary flux patch.

Required:

```text
I_dark = 0
```

in the ordinary-sector audit.

---

## Total Current-Flux Cancellation Warning

If all non-A current fluxes are summed:

\[
I_{\rm total}
=
I_H+I_V+I_{\rm boundary}+I_{\rm curv}
+I_{\rm dark}+I_{\rm exch}+I_{\rm sub}.
\]

Then:

\[
\Phi_{\rm total}=I_{\rm total}.
\]

A total cancellation condition would be:

\[
I_{\rm total}=0.
\]

But this is not sector current silence.

Each ordinary-sector non-A current route must be:

```text
zero,
diagnostic / role-level,
or future theorem-routed as neutral transport.
```

---

## Rejected Current-Flux Repair Routes

The script rejected:

```text
far-zone current as mass flux,
boundary repair current,
exchange repair flux,
curvature rescue flux,
H flux insertion,
recovery-selected current flux.
```

These are governance exclusions. They prevent fake current silence but do not define neutral currents.

---

## What This Study Established

This study established the current-flux burden:

```text
I_V = 0
I_sub = 0
I_exch = 0
I_curv = 0
I_H = 0
I_boundary = 0
I_dark = 0
```

unless the current is:

```text
strictly diagnostic / role-level,
or future theorem-routed as neutral transport
with no mass, scalar, boundary, source, or recovery effect.
```

---

## What This Study Did Not Establish

This study did not derive:

```text
J_V,
J_sub,
J_exch,
J_curv,
H flux,
boundary current law,
dark current law,
neutral transport,
pure-wind neutrality,
exchange neutrality,
curvature-current neutrality,
H insertability,
parent field equation readiness.
```

It only records the far-zone and boundary current flux burden.

---

## Failure Controls

The boundary/current flux silence audit fails if later scripts allow:

1. Nonzero non-A far-zone current flux in ordinary exterior.
2. Total current cancellation \(\sum_i I_i=0\) to replace sector silence.
3. \(J_V\) flux while \(J_V\) remains undefined.
4. \(J_{\rm sub}\) pure wind to gravitate by existence.
5. \(J_{\rm exch}\) to repair scalar tails, boundaries, or source routing.
6. \(J_{\rm curv}\) to act as curvature rescue flux.
7. H flux to act as boundary counterterm or \(M_{\rm ext}\) correction.
8. Boundary current introduced after leakage appears.
9. Dark current to patch ordinary failure.
10. Recovery target to choose current coefficients or directions.

---

## Next Development Target

The next script should be:

```text
candidate_boundary_repair_route_exclusion.py
```

Purpose:

```text
Inventory and reject boundary repair mechanisms.
```

Expected role:

```text
requirements / governance audit;
not a boundary theorem.
```
