# candidate_identity_vs_repair_sieve — Result Note

## Result

`candidate_identity_vs_repair_sieve.py` separates legal shared-identity routes from repair routes.

Retained theorem routes:

```text
derived_exact_pair:
  common K/sign relation derived with no remainder

derived_gauge_exact_remainder:
  exact part nonphysical and physical remainder zero by theorem
```

Rejected routes:

```text
free_opposite_sign;
dropped_rho;
repair_current;
active_O_patch.
```

## Main Findings

This is the core governance sieve for Group 76.

The group may continue only through routes where the cancellation is derived from structure:

```text
exact-pair identity;
gauge-exact remainder with no physical remainder.
```

It may not choose opposite signs, discard the remainder, add a repair current, or patch with active `O`.

## Boundary

The sieve does not prove either retained route. It only classifies which routes are legal.

## Steering Consequence

Proceed to final route classification. The summary should say that the exact-pair scaffold exists, `rho` is the obstruction, and no shared lift identity has been derived.
