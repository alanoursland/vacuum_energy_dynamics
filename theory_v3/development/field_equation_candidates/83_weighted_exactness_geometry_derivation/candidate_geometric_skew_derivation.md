# candidate_geometric_skew_derivation — Analysis Note

## Result

`candidate_geometric_skew_derivation.py` derives the skew from the parity moment ratio.

The surviving moment integrals are:

```text
A = ∫ J1 dy = 1024/3465
B = ∫ yJ0 dy = -512/1155
```

The measure-gradient pairing is:

```text
1024 ell(2R c - 3ell)/3465
```

The skew derived from the moment ratio is:

```text
c = -ell B/(R A)
  = 3ell/(2R)
```

The target check gives:

```text
difference from 3ell/(2R) = 0
```

## Interpretation

This is the main technical result of Group 83.

Group 82 found:

```text
c = 3ell/(2R)
```

as the value that cancels the weighted charge. Group 83 shows that, within the reduced weighted-exactness model, the same value is forced by measure-gradient orthogonality and parity moment balance.

That is a real improvement in status.

The coefficient now has an interpretation:

```text
c is the finite-thickness skew required to make the compact-support exact flux orthogonal to the measure gradient.
```

That means the skew is not repair paint inside this reduced model. It is the unique balance between:

```text
the even flux component controlled by c
```

and:

```text
the odd measure-gradient contribution generated by ell/R.
```

## What Changed

The Group 82 status:

```text
WEIGHTED_SKEW_CONDITION_FOUND_AS_COMPATIBILITY
```

should now become:

```text
WEIGHTED_SKEW_DERIVED_IN_REDUCED_CLASS
```

with strict scope.

That is the real progress of Group 83.

## What Did Not Change

The scope is still reduced:

```text
one-dimensional layer coordinate;
specific compact-support envelope f,w;
linear skew family;
quadratic geometric measure;
not full covariant lift theorem.
```

The result also does not address:

```text
local rho nonzero;
payload inertness;
D_layer legitimacy;
parent divergence;
recombination.
```

## Steering Consequence

The next pressure point is not this coefficient anymore. Inside this model, the coefficient has been paid for.

The next pressure points are:

```text
shape-family robustness;
covariant origin of f,w,mu;
local rho inertness/payload.
```
