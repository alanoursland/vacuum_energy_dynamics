# Candidate Curvature Accounting Mass Neutrality

## Canonical Filename

```text
candidate_curvature_accounting_mass_neutrality.md
```

This document summarizes the output of:

```text
candidate_curvature_accounting_mass_neutrality.py
```

## What This Document Is

This document is the Group 21 curvature-accounting mass-neutrality companion artifact.

It is not a curvature dynamics derivation, not a curvature current definition, not an anti-singularity theorem, not a correction-tensor insertion license, and not a parent field equation.

Its locked-door question was:

```text
Can curvature admissibility or e_curv affect exterior mass?
```

The answer is conservative:

```text
No curvature-accounting object is licensed here as an ordinary exterior mass carrier.

A_curv remains diagnostic / branch-filter.
e_curv remains diagnostic / accounting only.
J_curv remains unresolved.
Curvature balance remains theorem-targeted.
H_curv remains non-insertable.
```

Tiny goblin label:

```text
Curvature can count cracks.
It may not mint mass coins.
```

---

## Archive Status

The run had clean upstream archive dependencies:

```text
JV_mass_neutrality_inventory_dependency_21: dependency_satisfied
JV_scalar_residue_flux_dependency_21: dependency_satisfied
A_sector_mass_definition_dependency_21: dependency_satisfied
```

No operational dependency error was present.

---

## Core Reduced Diagnostics

The script derived three reduced danger witnesses.

### 1. Curvature scalar residue

For a generic curvature diagnostic residue:

\[
\phi_{\rm curv}(r)=\frac{C_{\rm curv}}{r},
\]

the reduced surface flux is:

\[
F_{\phi {\rm curv}}
=4\pi r^2 \phi'_{\rm curv}
=-4\pi C_{\rm curv}.
\]

If incorrectly treated as an A-like mass contribution, the mass-equivalent diagnostic is:

\[
\delta M_{\rm curv}^{\rm like}
=\frac{c^2}{8\pi G}F_{\phi {\rm curv}}
=-\frac{C_{\rm curv}c^2}{2G}.
\]

This is a danger diagnostic only. It is not a curvature mass law.

### 2. `e_curv` source-reservoir diagnostic

If curvature accounting were incorrectly allowed to induce an A-like exterior tail:

\[
\delta A_e(r)=\frac{q_e}{r},
\]

then:

\[
\delta F_A|_{e_{\rm curv}}
=4\pi r^2\delta A'_e
=-4\pi q_e,
\]

and:

\[
\delta M_A|_{e_{\rm curv}}
=\frac{c^2}{8\pi G}\delta F_A
=-\frac{c^2 q_e}{2G}.
\]

Therefore `e_curv` cannot become an exterior source reservoir by vocabulary.

### 3. `J_curv` far-zone flux diagnostic

For a generic far-zone radial curvature-current profile:

\[
j_{\rm curv}^r=\frac{I_{\rm curv}}{4\pi r^2},
\]

the sphere flux is:

\[
\Phi_{J_{\rm curv}}=4\pi r^2 j_{\rm curv}^r=I_{\rm curv}.
\]

A nonzero far-zone curvature-current coefficient is a mass-leak danger unless a future curvature-current law derives neutral transport.

The role-level curvature balance target is:

\[
S_{\rm curv}-R_{\rm curv}=0,
\]

but only after source, relaxation, orientation, domain, and boundary operators exist.

---

## Curvature Accounting Ledger

| Entry | Sector | Status | Consequence |
|---|---|---|---|
| C1 | `A_curv` diagnostic branch-filter | DIAGNOSTIC_ONLY | Curvature admissibility may filter branches only if it does not become dynamics by vocabulary. |
| C2 | `A_curv` scalar residue neutrality | THEOREM_TARGET | Curvature diagnostics must not leave hidden scalar mass tails. |
| C3 | `e_curv` accounting-only status | ACCOUNTING_ONLY | Curvature accounting cannot become mass source by naming it energy. |
| C4 | `e_curv` A-tail danger | REJECTED | `e_curv` cannot become a second exterior A-tail source. |
| C5 | `J_curv` unresolved current | UNRESOLVED | `J_curv` cannot carry ordinary exterior mass while undefined. |
| C6 | `J_curv` far-zone flux neutrality | THEOREM_TARGET | Curvature flux is not an ordinary mass flux coin. |
| C7 | curvature balance role target | THEOREM_TARGET | Curvature balance cannot tune `M_ext`. |
| C8 | finite admissibility / anti-singularity language | SAFE_IF | Anti-singularity language remains diagnostic until real current/source law exists. |
| C9 | curvature boundary behavior | THEOREM_TARGET | Curvature boundary behavior cannot be a boundary purse. |
| C10 | `H_curv` dependency | NOT_INSERTABLE | Correction tensors cannot be inserted from curvature accounting language. |

---

## Rejected Curvature Mass Routes

The script rejected the following routes:

```text
e_curv source reservoir,
curvature balance as mass repair,
J_curv gradient by fiat,
branch-kill called bounce,
H_curv curvature rescue,
recovery-chosen curvature route.
```

These are governance rejections backed by reduced danger diagnostics. They are not claims that a future, fully derived curvature sector is impossible.

---

## Required Future Burden

Curvature-accounting mass neutrality remains open. A future theorem must show:

```text
C_curv = 0 for scalar residue silence,
q_e = 0 for no e_curv A-tail source-reservoir effect,
I_curv = 0 for no independent far-zone curvature-current flux,
A_curv and e_curv remain diagnostic/accounting unless source-neutral dynamics are derived,
J_curv, curvature balance, and boundary behavior remain theorem-targeted,
no mass repair, source reservoir, bounce-by-name, H_curv rescue, or recovery-chosen curvature route.
```

---

## What This Study Established

This study established reduced diagnostic witnesses for curvature mass leakage:

\[
\phi_{\rm curv}=\frac{C_{\rm curv}}{r}
\quad\Rightarrow\quad
F_{\phi {\rm curv}}=-4\pi C_{\rm curv},
\]

\[
\delta A_e=\frac{q_e}{r}
\quad\Rightarrow\quad
\delta M_A|_{e_{\rm curv}}=-\frac{c^2 q_e}{2G},
\]

\[
j_{\rm curv}^r=\frac{I_{\rm curv}}{4\pi r^2}
\quad\Rightarrow\quad
\Phi_{J_{\rm curv}}=I_{\rm curv}.
\]

It also recorded that curvature balance is only a role-level theorem target until its operators are defined.

---

## What This Study Did Not Establish

This study did not define curvature dynamics.

It did not define `J_curv`.

It did not make `A_curv` a source.

It did not make `e_curv` source energy.

It did not derive curvature boundary neutrality.

It did not make finite admissibility into bounce dynamics.

It did not make `H_curv` insertable.

It did not make the parent field equation ready.

---

## Next Development Target

The next script should be:

```text
candidate_correction_tensor_mass_neutrality_guard.py
```

Purpose:

```text
Show why H_curv and H_exch remain non-insertable unless future tensor definitions prove source separation, divergence safety, scalar trace neutrality, boundary neutrality, far-zone flux neutrality, and A-sector mass neutrality.
```
