# Candidate Source Routing No Double Counting

## Canonical Filename

```text
candidate_source_routing_no_double_counting.md
```

This document summarizes the output of:

```text
candidate_source_routing_no_double_counting.py
```

## What This Document Is

This document is the ninth artifact for `21_source_routing_and_mass_neutrality/`.

It is not a parent source law, not a stress-energy coupling theorem, not a pressure law, not a current law, not a tensor equation, and not a source projector.

Its purpose is to consolidate the Group 21 ordinary source-routing discipline after the A-sector mass-charge definition, non-A inventory, scalar-tail audit, boundary audit, zeta/kappa audit, vacuum-current audit, curvature-accounting audit, and correction-tensor guard.

The locked-door question was:

```text
Can ordinary matter be routed without being counted multiple times?
```

The result is:

```text
Ordinary rho / M_enc remains routed to the A-sector mass charge.

Non-A sectors may not also count the same ordinary matter as independent mass,
source reservoirs, scalar tails, correction tensors, exchange roles, or dark patches.

Source routing remains constrained but not parent-derived.
```

Tiny goblin label:

```text
One mass coin. No duplicate pockets.
```

---

## Archive Dependency Status

The run reported a clean archive dependency check:

```text
correction_tensor_guard_dependency_21: dependency_satisfied
H_scalar_trace_leakage_dependency_21: dependency_satisfied
A_sector_mass_definition_dependency_21: dependency_satisfied
```

So the source-routing audit was connected to the prior correction-tensor guard and the A-sector mass reference.

---

## Reduced Danger Witnesses

The script recorded three reduced danger witnesses.

### Duplicate scalar tail

If ordinary source content is duplicated into a non-A scalar exterior tail,

\[
\phi_{\rm dup}(r)=\frac{C_{\rm dup}}{r},
\]

then

\[
F_{\rm dup}
=
4\pi r^2 \phi_{\rm dup}'(r)
=
-4\pi C_{\rm dup}.
\]

The A-like mass-shift diagnostic is:

\[
\delta M_{\rm dup}^{\rm like}
=
\frac{c^2}{8\pi G}F_{\rm dup}
=
-\frac{C_{\rm dup}c^2}{2G}.
\]

This is not a licensed duplicate source law. It is a warning that any duplicated ordinary scalar tail with \(C_{\rm dup}\neq0\) behaves like a second mass coin.

### Duplicate A-tail

If a non-A source-routing branch induces an extra A-like exterior tail,

\[
\delta A_{\rm dup}(r)=\frac{q_{\rm dup}}{r},
\]

then

\[
\delta F_A|_{\rm dup}
=
4\pi r^2\delta A_{\rm dup}'(r)
=
-4\pi q_{\rm dup},
\]

and

\[
\delta M_A|_{\rm dup}
=
-\frac{c^2q_{\rm dup}}{2G}.
\]

This shows why non-A branches may not induce \(q_{\rm dup}/r\) A-tails by routing vocabulary.

### Extra source-load ledger

The script represented the protected A-sector ordinary source as \(\rho_A\), then named possible duplicate non-A loads:

```text
rho_kappa
rho_zeta
rho_curv
rho_H
rho_exch
rho_dark
E_accounting
```

The extra non-A source load was:

\[
\rho_\kappa+\rho_\zeta+\rho_{\rm curv}+\rho_H+\rho_{\rm exch}+\rho_{\rm dark}+E_{\rm accounting}.
\]

The cancellation condition

\[
\rho_\kappa+\rho_\zeta+\rho_{\rm curv}+\rho_H+\rho_{\rm exch}+\rho_{\rm dark}+E_{\rm accounting}=0
\]

was explicitly rejected as a substitute for sector-by-sector no-double-counting.

Group 21 does not accept cancellation ledgers as neutrality. Each non-A pocket must be empty, diagnostic, non-metric, role-level, or theorem-routed.

---

## Ordinary Source-Routing Ledger

| Entry | Route | Status | Consequence |
|---|---|---|---|
| SRD1 | \(\rho/M_{\rm enc}\rightarrow\) A-sector mass charge | PROTECTED | A-sector remains the only currently licensed ordinary exterior mass coin |
| SRD2 | longitudinal current \(\rightarrow\) scalar continuity / density redistribution | THEOREM_TARGET | longitudinal flow needs continuity discipline before mass-routing use |
| SRD3 | transverse current \(\rightarrow W_i\) vector sector | SAFE_IF | vector routing is safe only if non-scalar and mass-neutral |
| SRD4 | TT stress / quadrupole \(\rightarrow h_{\rm TT}\) | THEOREM_TARGET | tensor/radiative routing remains separate from ordinary exterior mass charge |
| SRD5 | pressure / trace \(\rightarrow\) diagnostic or non-metric kappa/zeta relaxation only if neutral | THEOREM_TARGET | pressure/trace cannot become a second scalar mass source by declaration |
| SRD6 | zeta/kappa residuals excluded from ordinary source roles | PROVISIONAL | residual-kill/non-metric residual remains safest but not a theorem |
| SRD7 | curvature diagnostics excluded from ordinary matter source roles | DIAGNOSTIC_ONLY | curvature accounting remains diagnostic, not source energy |
| SRD8 | correction tensors excluded from ordinary matter source roles | REJECTED | H tensors cannot be ordinary matter sinks or mass patches |
| SRD9 | exchange roles remain separate from ordinary matter routing | ROLE_LEVEL | exchange is not ordinary matter rerouting and not repair |
| SRD10 | dark-sector labels optional downstream only | DEFER | dark sector is not an ordinary mass patch |
| SRD11 | energy/accounting terms remain diagnostic/accounting | ACCOUNTING_ONLY | energy words do not create source channels |
| SRD12 | ordinary scalar radiation rejected | REJECTED | ordinary scalar radiation remains outside the current source-routing license |

---

## Rejected Source Double-Counting Routes

The script rejected these routes:

```text
rho_to_kappa_mass_charge
pressure_trace_scalar_charge
ordinary_T_to_Sigma_exch
curvature_diagnostics_as_sources
H_absorbs_ordinary_source_mismatch
dark_label_patches_ordinary_failure
source_cancellation_ledger
```

These are governance exclusions, supported by the reduced danger witnesses and by the Group 21 ledger discipline. They are not new field-equation theorems.

---

## What This Study Established

This study established the current ordinary source-routing discipline for Group 21:

```text
rho / M_enc -> A-sector mass charge

longitudinal current -> continuity / density redistribution only if derived

transverse current -> W_i without scalar mass duplication

TT stress / quadrupole -> h_TT without scalar mass duplication

pressure / trace -> diagnostic or non-metric kappa/zeta relaxation only if neutral

ordinary scalar radiation -> rejected

ordinary matter -> not rerouted into J_sub/J_exch/Sigma_exch/H_exch/H_curv
```

The script also recorded the danger witnesses:

```text
duplicate scalar tail C_dup/r -> F_dup = -4*pi*C_dup

duplicate A-tail q_dup/r -> delta_M_A|dup = -c^2*q_dup/(2G)

extra source-load ledger -> non-A source labels must not survive by cancellation
```

---

## What This Study Did Not Establish

This study did not derive parent source routing.

It did not derive a covariant source projector.

It did not derive the pressure/trace law.

It did not define \(J_{\rm sub}\), \(J_{\rm exch}\), \(H_{\rm curv}\), \(H_{\rm exch}\), or a dark-sector source law.

It did not prove that every future non-A source channel is impossible.

It only recorded the current no-double-counting burden:

```text
ordinary matter must not be duplicated into independent non-A mass/source channels by declaration.
```

---

## Governance Status

The run recorded:

```text
source_routing_no_double_counting_marker_21
```

as the inventory marker.

It also recorded the reduced diagnostic derivations:

```text
duplicate_scalar_tail_flux_21
duplicate_scalar_tail_mass_shift_diagnostic_21
duplicate_A_tail_flux_diagnostic_21
duplicate_A_tail_mass_shift_diagnostic_21
ordinary_source_extra_load_ledger_21
```

The main open theorem target is:

```text
ordinary_source_no_double_counting_theorem_21
```

---

## Failure Controls

The source-routing audit fails if later scripts allow:

1. \(\rho/M_{\rm enc}\) to source a second ordinary exterior mass channel by declaration.
2. \(\rho\) to also source kappa, zeta, curvature, H, exchange, or dark labels.
3. Pressure trace to create exterior kappa/zeta scalar charge.
4. Ordinary \(T_{\mu\nu}\) to become \(\Sigma_{\rm exch}\), \(J_{\rm exch}\), \(H_{\rm curv}\), or \(H_{\rm exch}\) by convenience.
5. Curvature diagnostics or accounting terms to become source reservoirs.
6. H tensors to absorb ordinary source mismatch or define their own source by divergence.
7. Dark labels to patch ordinary source, boundary, scalar-tail, H, or recovery failure.
8. Zeta/kappa residuals to become a second scalar metric source.
9. Source cancellation ledgers to stand in for sector-by-sector neutrality.
10. O/source projectors to enforce routing without domain, kernel, image, and boundary law.

---

## Next Development Target

The next script should be:

```text
candidate_group_21_source_routing_status_summary.py
```

Purpose:

```text
Close Group 21 by summarizing A-sector mass charge, non-A neutrality,
scalar-tail flux, boundary mass preservation, zeta/kappa neutrality,
vacuum-current neutrality, curvature neutrality, correction-tensor neutrality,
ordinary source routing, and no-double-counting guardrails.
```
