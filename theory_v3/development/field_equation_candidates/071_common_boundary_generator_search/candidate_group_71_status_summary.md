# candidate_group_71_status_summary — Result Note

## Result

`candidate_group_71_status_summary.py` closes Group 71 as a controlled generator-search obstruction.

Stable result:

```text
generator criteria explicit;
orientation anti-match compatibility explicit;
component anti-match compatibility explicit;
free-parameter / renamed-B / repair routes rejected;
D_layer legitimacy remains unresolved;
L_bulk and L_gauge remain lift-cleanliness obligations;
no strong common generator established in tested classes (not a no-go theorem);
boundary-lift matching route remains partial theorem target / controlled obstruction;
parent divergence identity remains unproven;
recombination remains blocked.
```

## Analysis

This is a good Group 71 closure. It answers the central question:

```text
Are the Group 70 signs carved by geometry, or only knobs selected to stop the leak?
```

Current answer:

```text
not yet carved by geometry in the tested classes (not a no-go theorem).
```

But it does not overstate the negative result. The group did not prove that no common generator can exist. It did not establish a strong generator in the tested classes, and therefore the route must be split into more precise burdens.

The summary correctly preserves all major boundaries:

```text
no renamed B;
no chosen signs;
no diagnostic layer;
no active O by label;
no parent equation.
```

It also identifies the two concrete technical blockers that should guide Group 72:

```text
D_layer legitimacy;
covariant lift neutrality for L_bulk and L_gauge.
```

## Boundary

No parent divergence identity is proven. No recombination is licensed. No parent equation is written.

## Unexpected Results

None. The group landed in the expected controlled-obstruction region.

## Steering Consequence

The best next group should not be another broad common-generator search unless a new candidate family is available. The next work should split the blockers:

```text
72_layer_term_legitimacy_audit
```

if the project wants to resolve whether `D_layer` can be a legitimate boundary component, or:

```text
72_covariant_lift_neutrality_attempt
```

if the project wants to attack `L_bulk = 0` and `L_gauge = 0`.

A route-management group could also formalize:

```text
boundary-lift matching route = compatibility-only unless split theorem targets are later solved.
```
