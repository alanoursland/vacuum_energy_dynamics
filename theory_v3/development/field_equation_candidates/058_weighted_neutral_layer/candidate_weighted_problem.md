# candidate_weighted_problem — Result Note

## Result

The opener correctly frames Group 58 as a weighted-neutral finite-layer construction group.

It directly targets the sharp blocker from Group 57:

```text
flat odd cancellation is not enough;
spherical r^2 weighting must be explicit.
```

The group is allowed to construct a nontrivial reduced layer profile with zero weighted scalar charge, but it is not allowed to treat that profile as insertion, ordinary source, or parent-equation structure.

## Main Findings

The opener preserves the right discipline:

```text
weighted neutrality integral uses r=R+ell*y;
constructive profile search is allowed;
physical use remains blocked.
```

It rejects the three dangerous shortcuts:

```text
flat neutrality;
neutrality by naming;
profile as ordinary source.
```

That matters because Group 58 should not merely replace one unproven assumption with another. The weighted charge must actually be computed.

## Boundary

No profile is constructed in this opener. No insertion, active `O`, recombination, or parent closure is opened.

## Steering Consequence

The group should proceed to construct a concrete weighted-neutral profile. The profile must be nontrivial, endpoint-localized, and exactly neutral under the spherical weighting.
