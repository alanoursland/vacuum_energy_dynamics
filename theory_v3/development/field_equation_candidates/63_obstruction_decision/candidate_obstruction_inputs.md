# candidate_obstruction_inputs — Result Note

## Result

The script restates the Group 62 obstruction inputs that control the Group 63 status decision.

It records the closure algebra:

```text
T = P*(1-gamma)
A = P*(gamma+1)
```

with:

```text
trace-free gamma = 1
active-mass-neutral gamma = -1
```

It also restates the pressure-sum witness:

```text
P(0) =
-4R^2*p0*(7R^2 - 2ell^2)/(7R^2 + ell^2)^2
```

and the integrated accounting:

```text
I_P/E_pr = 2
```

## Main Findings

This grounds the decision surface.

The obstruction is not merely verbal. It has three concrete components:

```text
trace-free and active-mass-neutral closures require opposite gamma values;
P is not generically zero;
I_P is tied to E_pr by I_P=2E_pr.
```

The script also preserves the amplitude burden:

```text
p_free remains underived.
```

So the candidate cannot be promoted without ignoring the actual Group 62 results.

## Boundary

This restates reduced obstruction inputs. It is not a stress theorem and not a new closure.

## Steering Consequence

The next step is to classify allowed and rejected statuses. The decision should be based on these obstruction inputs, not on whether the candidate is aesthetically appealing.
