# candidate_escape_hatch_requirements — Result Note

## Result

The script separates legitimate future routes from shortcut escapes.

Potentially legitimate future routes:

```text
variational stress origin;
compensation/inertness principle;
covariant layer lift;
modified closure relation;
new tensor sector.
```

Rejected shortcut escapes:

```text
choose u=P;
choose u=-P;
declare P=0;
set p_free=0;
hide load in active O.
```

## Main Findings

This script is a useful route filter.

The future routes are not accepted as solutions. They are only allowed as future theorem targets if they actually derive the missing structure.

For example:

```text
variational stress origin
```

would need to derive `u`, `p_free`, and closure.

```text
compensation/inertness
```

would need to explain mass/trace neutrality without repair.

```text
covariant layer lift
```

would need to lift reduced diagnostics into geometry and cannot by itself erase the stress-accounting obstruction.

The rejected routes are exactly the common shortcut traps.

## Boundary

No escape route is solved here.

## Steering Consequence

The classifier should preserve the final Group 63 status: no promotion, diagnostic downgrade allowed, conditional retention allowed only under contract, and physical use blocked.
