# 04 — The Field Equations

**Admitted to the theory by owner decision, 2026-06-12.** This section
presents the field equations of Vacuum Energy Dynamics as established
results, promoted from `development/field_equation_trials/` upon
fulfillment of that program's charter. Provenance for every claim lives
in the trials' forge scripts and lab reports; this section is the
statement of record.

## The result in one paragraph

From the adopted postulate set (P1–P6, P7′, P9) the vacuum's
gravitational response is derived, sector by sector, with zero matched
coefficients and zero structural placeholders. The derived equations
are Einstein's:

$$G_{ab} + \Lambda g_{ab} = \frac{8\pi G}{c^4}\,T_{ab}$$

This was not the goal and it was not assumed; it is what survived. Every
candidate deviation the theory proposed was given a gate with a kill
condition, and every one died honestly. VED therefore *contains* general
relativity as its gravitational sector — derived from a vacuum-substance
ontology rather than postulated — and its remaining novelty lives
entirely outside the metric response: in Λ's origin, the dark sector,
the substance frame, and the interior cap.

## Reading map

| file | contents |
|---|---|
| **`proof.md`** | **the direct derivation: postulates → field equations, ordered so GR is never imported to derive GR; anti-circularity ledger; verification index** |
| `01_the_field_equations.md` | full statement; sector table; every coefficient and where it was derived |
| `02_derivation_chain.md` | the logical chain from postulates to equations, with provenance pointers |
| `03_sector_architecture.md` | signs, stability, and why the scalar sector must be a constraint |
| `04_divergences_from_gr.md` | where VED is *not* GR: ontology, substance frame, predictions ledger, falsifiers |
| `05_open_obligations.md` | rigor debts on the closed result; handoff to the vacuum-sector program |

`proof.md` is the document to read for confidence in the result; the
numbered files are the reference statement, the chain map, the sector
architecture, the divergences, and the debts respectively.

## Status and conditionality (stated once, here)

The result is theorem-grade at reduced level (static spherical,
linearized radiative, stationary vector, quasi-static cosmological) and
conditional at covariant level: the closure to the Einstein-Hilbert form
rests on the conditional-response gates (G15/G16/G20 lineage) plus the
uniqueness of the self-coupled spin-2 closure (Deser 1970, cited; in-house
proof an open obligation). These are rigor debts, not freedoms: no
coefficient can move while they are paid down. See
`05_open_obligations.md`.

## Relation to the rest of the theory

- `01_postulates/` is the input. Nothing here was adopted to force the
  output; the recovery-shaped postulates were eliminated during the
  program (P7 demoted to shadow, P8 reduced to a theorem).
- `03_weak_field/` (T5–T9) is subsumed: the weak-field recoveries are
  now corollaries of the full equations rather than independent targets.
- `development/field_equation_trials/` is the lab notebook: charters,
  forge scripts, lab reports, kill ledger. Closed by
  `99_closing_report.md` there.
- The successor program (vacuum sector) inherits the open physics:
  Λ, dark sector, measure identity, interior.
