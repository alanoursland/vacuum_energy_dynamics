# Paper 5 TODO

## Literature and positioning

- Search beta/Jacobi moment literature for adjacent moment ratios and endpoint
  contact hierarchies.
- Search Gram and cross-Gram matrix literature for the cleanest terminology.
- Check whether this result is best positioned as a short mathematical note,
  an appendix to another paper, or a standalone math-physics paper.
- Add BibTeX and avoid novelty claims until the search is complete.

## Proof cleanup

- Define the admissible finite space `H_{R,N}` once and use that notation
  throughout.
- Prove the row family is a basis of `H_{R,N}` for all `R,N`.
- Prove the balanced source family is a basis of the same `H_{R,N}` for all
  `R,N`.
- Decide whether the final cross-Gram proof should use basis change or the
  annihilator argument.
- Make the relation between the `C_R` weight and the projection pairing weight
  `W_R` convention-clean.

## Verification

- Collect the symbolic verification scripts into a reproducible package.
- Replace the internal-path verification appendix with exact commands and
  expected outputs.
- Add a small exact determinant table only as diagnostic evidence, not as the
  proof.

## Figures and tables

- Check figure labels at journal column widths.
- Add Table 1: first few `R` levels and ratios.
- Add Table 2: proof objects, roles, and assumptions.
- Add Table 3: determinant evidence versus structural proof.

## Release

- Add a standalone notation preamble.
- Add references.
- Decide target venue after the literature pass.

