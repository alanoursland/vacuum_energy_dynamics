# 6. Linear Spin-2 Closure and Radiative Bootstrap

The kinematic layer gives a symmetric tensor perturbation, universal matter
response, and relabeling gauge freedom. In flat background field theory, the
unique local second-order gauge-invariant linear operator for a massless
symmetric tensor is the Fierz-Pauli operator. This is a special-relativistic
field-theory uniqueness result, not an import of gravitational phenomenology.

The linear theory cannot fix its own normalization. If the transverse-traceless
kinetic coefficient is called `K_T`, radiative observables scale with the
combination of matter coupling and `K_T`; linear energy balance holds for a
family of normalizations. P9 must do the work that the linear theory cannot.

P9 requires the field's own energy to gravitate at the universal coupling,
counted once. Iterating that self-coupling closes the massless spin-2 theory
on the Einstein response, up to the standard covariant uniqueness theorem. The
normalization is then fixed by the static law:

```text
N = c^4 / (8 pi G) .
```

The transverse-traceless kinetic coefficient is fixed by the radiative
self-coupling:

```text
K_T = c^4 / (16 pi G) .
```

The corresponding wave energy density is

```text
<u> = c^2/(32 pi G) <dot h_ij dot h_ij> .
```

The quadrupole power coefficient follows:

```text
P = (G / 5 c^5) <dddot Q_ij dddot Q_ij> .
```

Binary-pulsar and gravitational-wave observations are not used to tune these
numbers. They are kill conditions for the derived radiative normalization.

The covariant closure presently relies on the standard self-coupled spin-2
uniqueness route. Replacing that citation with an in-house proof is a rigor
debt, not a remaining coefficient freedom.
